# Accepted-residual register

Consciously accepted tradeoffs, verified and argued once so reviews stop
re-flagging them. Cite entries by ID in review output ("suppressed per
TC-003"). Rules:

- **Adding**: a review that ACCEPTS a new residual adds the entry in the
  same PR (id, date, one-line risk, why accepted, where argued). Without
  the entry, the next review re-litigates it.
- **Pruning**: each entry names the code it describes; if that code is gone
  or the tradeoff is fixed, delete the entry in the fixing PR.
- **Challenging**: entries are decisions, not laws. To overturn one, argue
  it where it was decided — not by re-flagging it in review. Precedent:
  the `ENOTFOUND` failover below was carried as a residual for exactly one
  round, then overturned on its merits (a DNS failure cannot be
  account-specific, so failing over burns the fleet and makes a network
  blip present as an outage) and fixed. That is the intended lifecycle.

| ID | Residual | Why accepted | Argued at | Added |
|---|---|---|---|---|
| TC-001 | `ctx.tried` / `ctx.reauthed` / `ctx.pinnedIndex` hold account indices for the life of a request; a removal mid-request leaves them naming a different account (`tried` excludes the wrong one, `pinnedIndex` forces whoever took the slot) | Narrow window, impact is one misrouted request and no data loss. `removed.index = -1` closes the late-call half. Closing the rest needs re-resolution by name per attempt (the MITM pin already does this) or a generation stamp on the account list — a design change to request handling that wants its own review round | Round 5, user decision | 2026-08-18 |
| TC-002 | The 60s stuck-rollover log throttle (`_noteStuckRollover`) is untested — widening it to 600s breaks nothing | It reads the wall clock where the rest of the tracker takes an injectable `now`; adding a clock parameter for one log throttle is a source change that exists only to be tested. What IS held: the line's content and its per-(account, bucket) key | Round 5 (AM49) | 2026-08-18 |
| ~~TC-003~~ **RESOLVED** | About ten fixtures sat exactly on the pressure band's tolerance boundary — the recurring `a{0.5, 50h} / b{0.1, 60h}` pair, where `0.5/(50·3600)` is *exactly* `(0.9/(60·3600))/1.5` in float | Accepted at the time because rewriting them late in a branch was churn. **Gone, as a side effect rather than a decision**: the pressure-before-reset tiebreak inverted the opening premise of those same fixtures, so their inputs were rewritten to make the intended account the preferred one, and the boundary pair went with them. 46 occurrences across three files; 45 fell to that rewrite and the 46th survived it — named `c` rather than `a`, so a substitution keyed on the name missed it — and was removed on its own. The file header now records the shape to avoid rather than the hazard to live with | Round 4 user decision; resolved round 2 of the capacity delta | 2026-08-21 |
| TC-004 | The first time an account reports family utilization, a genuine roll of the *shared* window coincident with that flip is not seen by the family bucket | The shared bucket has its own baseline and detects the same roll, so the session still preempts, and from that point the family bucket has its own window governing it — which is the right answer going forward. One missed preemption, on one bucket, once, at a real transition. Closing it means comparing across windows, which invariant 2 forbids | Round 5 (Case A), measured with `repro-coincident.mjs` | 2026-08-18 |
| TC-005 | A handful of mutation-table rows survive as **equivalent mutants** — unobservable given the rest of the code, not untested. Currently: the advisor predicate asserted on the session path (no path can produce the divergence today), the `_isActive` guard in `_pinsInclude` (implied by `lastSeen >= pin.at`), and — in `_releaseHold`'s `if (!hold \|\| hold.rid !== s.rid \|\| !s.holds.delete(hold)) return;` — the MIDDLE clause only. Name the clause, not the line: `!hold` is a live null guard (`endRequest` reaches here with a null hold on the lost-hold path), and the `s.holds.delete(hold)` CALL is load-bearing because its side effect removes the hold from the set. `hold.rid !== s.rid` is unreachable: `s.holds` has exactly one insertion site, `beginRequest`, which stamps the record's own rid, so the set cannot contain a foreign hold, and `endRequest` has already returned on one before calling. Measured separately, the early RETURN on delete's false result is also unobservable — a second release finds `hold.buckets` already emptied by `hold.buckets.clear()`, so the decrement loop is a no-op with or without it — but do not simplify the expression on that basis, because the call must stay. Becomes observable on a third caller or if `endRequest`'s guard is relaxed; the seam row was withdrawn because a row that cannot die measures nothing. **A SECOND ROW WAS WITHDRAWN ON THIS PRECEDENT AT `e51b405`, AND THE REASON IS A PROPERTY OF THE FIX RATHER THAN A GAP IN IT.** The row *a scope with no representative is marked as one again* covered the shared scope's null representative. Under the invariant-derived predicate the shared scope carries `match: []`, so `scopeGlob` and `stripped` are BOTH null and `stripped !== scopeGlob` is `null !== null` — false. Its correctness became a STRUCTURAL CONSEQUENCE of the predicate rather than a guarded condition, and the conjunct that excludes it is the one the wildcard-free row already targets, so marking it would take two simultaneous edits and no single mutation can reach it. **THE TEST STAYS AND PINS THE PROPERTY** (*a scope with no representative is not marked as a synthesised basis*, with a live CONTROL asserting the route scope in the same payload IS still marked, so it cannot pass on a build that marks nothing, and a degeneracy guard that fails if the shared scope ever acquires a representative). The row measured nothing; the test measures the property. **The CLAIM boundary joined this entry in the round-3 pass-9 cycle**: fold-back 2 added `s.holds.has(hold)` to `touch`'s guard, and membership implies ownership by the same one-insertion-site argument, so `hold.rid === s.rid` there became unobservable too and its seam row was withdrawn in the same cycle. Found by the seam rather than by reading: the row went from dying to SURVIVES the run after the fold-back landed. Both guards stay in source — they are preconditions, not dead code — and the tripwire for both is a SECOND insertion site into any record's `holds`, at which point neither clause is implied and both rows should be restored | Each was argued individually and at least one was confirmed by 30,041 randomized trials against a generator *proven* able to produce the divergence elsewhere. **This list is anchor-relative and must be re-derived after any rewrite** — a previous list evaporated with the session that made it, and the tables reported zero survivors afterwards, so nobody could check it | Rounds 4-5 | 2026-08-18 |
| ~~TC-006~~ **OVERTURNED** | `res.destroyed` is inert on the HTTP/2 (MITM) path: `Http2ServerResponse` has no `destroyed` property at all | **Accepted on a premise that measurement refuted.** The entry argued the cost was ~1s of pulling a dead stream; at the RETRY guards the cost is quota — an abandoned MITM request walked the whole ladder (measured h1: 1 upstream call, h2: 4), and `:1023` re-enters `forwardRequest` without incrementing `retryCount`. Burning weekly quota on requests nobody is waiting for is the opposite of what this feature is for. Fixed: `clientGone()` covers the `admit` slot and six of the ladder's nine rungs — **not** "every rung", which this entry claimed for one round before anyone counted. The three bare rungs (`status === 'error'`, 403, transport-throw) are backstopped by `admit()`'s abort probe, measured at 1 of 4 accounts with it live against 4 of 4 inert, so the residual quota cost is zero. **The cost basis was wrong a second time.** `streamResponse`'s breaks were left bare on the grounds that they waste a read of a body whose quota is already spent. Measured on this tree: a cancelled h2 SSE stream leaves the handler in the backpressure wait, which listens for `drain` or `close` on a stream where `close` fired before the listener existed and `drain` never will. The handler does not return, its activity entry never closes, and the process does not exit — `sawChunk=true started=1 ended=0 returned=false`, killed at 90s. Both breaks now read through `clientGone()`, and `MITM h2: a cancelled stream does not strand the request handler` fails without them. The 429 answer guard named here as inert had already been converted when this entry was written. Still bare only at the opt-in egress wait, where a cancelled request waits out a bounded hold and spends nothing | Round 6, overturned on `p4-h2-abort.mjs` | 2026-08-19 |
| TC-007 | `otherHostAvailable` counts accounts selection can never choose — disabled, rate-limited, errored, model-ineligible. They never enter `ctx.tried`, so their vote for "another host is available" never expires | Scoped to `ENOTFOUND` and the other host-scoped codes, where it leaves cases merely unfixed. `ECONNREFUSED` is deliberately NOT routed through this condition for exactly this reason — it was unconditionally transient before, so a gap there is a regression rather than an unfixed case. Closing it properly means computing the vote over the same candidate set selection uses (`_isAvailable`), which is invariant 12 and its own change | Round 6, reproduced with `r6-failover-probe/d1-body.mjs` and re-confirmed on the DNS path (`r6-d1-dns.mjs`: 3 of 4 accounts marched, client told `rate_limit_error`) | 2026-08-19 |
| TC-008 | The vote cannot see the shared egress proxy every account dials through: when the proxy is the thing that is broken, accounts with different `upstream` values are still equally unreachable, and the vote says failover is worth taking | Same scoping as TC-007 — unfixed, not regressed. The condition asks about the target host and the proxy is a second hop it does not model | Round 6, `proxy-repro.mjs` case A | 2026-08-19 |
| TC-009 | Hosts are compared as raw config strings, so `localhost` vs `127.0.0.1`, `api.example` vs `API.EXAMPLE`, and `api.example` vs `api.example:80` each read as two different hosts and vote for a failover that dials the same place | Verified all three (`normalize.mjs`, re-run here). Normalizing needs a URL parse plus default-port and case rules, and every wrong answer is in the safe direction for `ENOTFOUND` — an extra failover attempt, not a lost one | Round 6, `normalize.mjs` | 2026-08-19 |
| TC-010 | A `/tc-acct/` pinned request votes for a failover `forwardRequest` will never perform: it burns a retry and then answers `429 Pinned account is unavailable` instead of closing the connection | Verified on the DNS path: one attempt, then the pinned-unavailable 429. One wasted retry and a misleading status on a request that is already pinned to one account; the client still retries | Round 6, re-confirmed with `r6-verify-residuals.mjs` | 2026-08-19 |
| TC-011 | Behind an egress proxy a DNS failure never reaches the classifier at all — `src/sx.js:85` and `:72`, and the `upstream-proxy` CONNECT path, throw code-less errors (`upstream proxy refused CONNECT: …`), so nothing in the error carries `ENOTFOUND` | Measured in this sandbox: the same unresolvable name yields `ENOTFOUND` direct and a code-less proxy error through a proxy. The classifier is correct; the information is destroyed one layer below it. Both DNS tests disable the proxy through `setUpstreamProxy` for this reason and assert they observed a resolution failure, so the environment cannot silently turn them green | Round 6, measured while fixing the tests | 2026-08-19 |
| TC-012 | An `AggregateError` whose children disagree is decided by the socket arm: it short-circuits over the whole code list, so one `ECONNRESET` child outvotes an `ENOTFOUND` sibling and the failover is removed | Verified (`r6-verify-residuals.mjs`). Only reachable when happy-eyeballs gets genuinely different failures per address, and the outcome — close and let the client retry — is the safe direction. Ordering the arms by severity would need a rule for which failure "wins" that nothing currently needs | Round 6 | 2026-08-19 |
| TC-013 | A mixed-backend fleet keeps its failover when a host REJECTs and loses it when a host DROPs: `EHOSTUNREACH` is host-scoped (conditional) while `ETIMEDOUT` is socket-scoped (unconditional), so the same dead host gives opposite answers depending on which one the kernel reports | Measured both ways. `ETIMEDOUT` is genuinely ambiguous — it is also what a live host under load produces, where failing over to a different backend is wrong — so classifying it host-scoped would trade this for a worse case. Splitting it needs something the error does not carry: whether the connection was ever established | Round 6, `isTransientUpstreamError` table | 2026-08-19 |
| TC-014 | Under `TEAMCLAUDE_UPSTREAM_GLOBAL_FETCH=1` the mixed-fleet escape hatch disappears: Node's global fetch reports everything as `TypeError: fetch failed`, and that arm returns transient BEFORE `otherHostAvailable` is consulted, so a DNS failure never fails over to an account on a different host | Measured: `fetch failed{cause:ENOTFOUND}` with another host available is transient, where a bare `ENOTFOUND` fails over. The arm is load-bearing for the legacy path, where the real code is only on `.cause` and dropping it would misclassify every error that path produces. Correct fix is to consult the cause's code first and fall through to the message arm, which is a change to the classifier's shape rather than a set membership | Round 6, `isTransientUpstreamError` table | 2026-08-19 |
| TC-015 | `forwardRequest`'s late 502 and the 429 answer now share `clientGone` with the outer catches, but nothing observes the difference: on h1 the two predicates agree, and on h2 a late write to a dead stream is a measured silent no-op either way | Kept because three predicates for one decision is the drift that produced two of this round's findings, not because a test can see it. Recorded so the next reviewer does not spend the time re-deriving that its mutation survives | Round 6 | 2026-08-19 |
| TC-016 | The transport-throw rung fails over without consulting `clientGone`, and unlike the other two bare rungs nothing has measured it: the fixture written for it never arrives, because `ECONNRESET` classifies as transient and short-circuits before the rung is reached | Unguarded **by reading only**. `admit()`'s probe backstops it on the same argument as the other two, but that argument is transferred, not measured here — recorded so the next reader knows which of the three bare rungs has evidence behind it and which has an analogy | Round 7 | 2026-08-20 |
| TC-017 | The listener's inner catch around `forwardRequest` writes its late 502 on a bare `!res.headersSent`, with no headers-sent destroy arm — the shape that produced three hangs elsewhere | No input is known to reach it with headers already sent; the review that found it explicitly declined to claim a hang and labelled the exposure reasoned rather than executed. Left alone rather than fixed on a hypothesis, because the same reasoning applied to `ECONNREFUSED` produced a regression in round 6 | Round 7 | 2026-08-20 |
| TC-018 | The unknown-pin 404 and the blocklist early return call `onRequestEnd` for a request id that never had an `onRequestStart`, so consumers see a close for a row they never opened | Pre-existing, not introduced by the error-path work, and both consumers tolerate it — the TUI renders `dur='?'` and headless ignores the unmatched id. Cosmetic; fixing it means either suppressing the close or opening a row for a request that was rejected before it began, and both are larger than the symptom | Round 7 | 2026-08-20 |

| TC-019 | The 5h bucket bounds how fast a weekly window can be drained. When `unified5hReset > unified7dReset` the current 5h window is the only one left before the weekly rolls, so the weekly tail beyond what that window can deliver expires unspent, and once the 5h gate trips the account serves nothing while still holding quota | Provider-imposed, and no ranking change recovers it: the router already sends 100% of sessions to the top-pressure account, so this is arithmetic rather than policy. Measured on the live fleet 2026-08-20 across a complete concentrate-gate-redistribute cycle: the top-pressure account entered with 20 points of weekly headroom, its 5h window resetting after its weekly, and finished at `7d = 0.86` with 0.85 h still on the weekly, so it spent 6 and **stranded 14 of the 20**. A mid-episode forecast put the stranding at 8-12, assuming a 0.33-0.50 weekly-per-5h conversion; the realised exchange rate was **0.21**, below that band, and a worse conversion strands more. The forecast was optimistic, and the range it produced should not be cited as the measurement. Feeding 5h into pressure does not help — same winner, same band, at the cost of a formula change that moves TC-003's boundary fixtures. **The zero-code lever is `tolerance`**, which trades expiry spend for throughput spread and is the right response when the symptom is gating rather than expiry. What would change this: the narrow case where a near-gate account wins the band, gates immediately, and costs one failover cycle. If that is ever observed mattering, the remedy is a 5h-aware *band* exclusion — a boolean over `unified5h`, `switchThreshold` and the two resets, not a pressure-formula change, so TC-003's fixtures stay put — and it wants its own review round | 2026-08-20, sampled `/teamclaude/status` every 30s against the live darwin pooler | 2026-08-20 |
| TC-020 | `removeAccount` establishes a current account by CLAMPING, not by ranking. When the removed account was both current and last, `currentIndex` is set to `accounts.length - 1` — whichever account happens to occupy the final slot. Reproduced with `verify-r2d-remove.mjs`: three accounts, current on the doomed last one, survivors holding 2.64e-5 and 8.33e-6 of expiring pressure; after removal `current` names the account with **a third of its peer's** expiring quota, `_pickBestAvailable` disagrees and names the other, and `getActiveAccount` serves what `current` names. `_preemptedBy` consults priority only, so within one tier nothing re-ranks it and the pointer stays until something else moves it | **Not a footnote to the pressure tiebreak, and not fixed by it.** It is a fourth writer of `currentIndex`, found by enumerating the invariant's sentence rather than the reported call sites, and it does not route through band membership under EITHER rule: on the pre-capacity tree the account it lands on is not in the band at all and is served anyway, because the clamp writes the index directly and the sticky path never consults the band. So it is independent of the capacity change rather than older than it. Reproduces identically on 35f156e, which is why it is not in this change: fixing it means giving the repair a ranked chooser, which is a behaviour change to account removal and wants its own review. Default disposition is residual; if overturned it becomes a separate small change rather than joining the routing commit | Round 2 of the capacity delta, `verify-r2d-remove.mjs` on both trees | 2026-08-21 |
| TC-021 | `an unexpected reply is reported as such, not as a down server` (`test/cli-switch.test.js`) fails under full-suite load. It spawns a CLI subprocess against a local impostor server, and under contention the subprocess reports a down server rather than the unexpected reply it is asserting on | **Pre-existing and unrelated to this change**: the file is untouched here and was last modified in `77593cc`, an unrelated CLI commit. Measured: 8 of 8 passing in isolation, failing only under full-suite load on a box at load average 12-16. Not fixed here because it is outside this change's surface and a timing fix wants its own review. **How it surfaced is the useful part**: the seam table's green-baseline preflight caught it on its second day, unprompted, in the protective direction — without it that single failure would have been credited to all 59 rows and the table would have printed a clean sheet and exited 0. It also NARROWS the residual the tool documents: the preflight closes the already-red case outright AND the subset of flakes that land in the baseline run, leaving only the subset that lands inside a row's own run, which the per-row subtraction cannot catch because that name was never in the baseline set | Round 3, seam preflight abort at `05f96ce`; 8 isolated repetitions | 2026-08-22 |
| TC-022 | Concurrency within ONE weekly family is still read as zero load. Pins are keyed by bucket, so two concurrent requests on the same family share a pin and the second RE-PINS it: the first account's pin is destroyed rather than aged out, and a hold cannot rescue a pin that no longer exists. Reproduced: `inFlight=2`, `pins=unified7d->acct1`, acct0 reading `{sessions:0, context:0, reports:0}` with a live 100000-token stream on it | **The symptom is pre-existing** — it reproduces identically on `35f156e` — **but the narrowed claim is new**, so this is not simply inherited: the per-pin holds were built to stop a live stream reading as zero and cover the CROSS-family case only. That narrowing is now stated in the successor's commit message, at `_pinCounts` in `src/session-tracker.js`, and in `docs/routing.md`. Closing it means a pin per REQUEST rather than per bucket, which is a different change. **A future overlap counter used as evidence for this entry must key on the (session, bucket) PIN, not on distinct accounts.** An account-keyed crossing measures distribution — how work spreads across the fleet — not this exposure, which is two requests contending for one pin. The earlier ruling that the concurrency observable count "in-flight requests on more than one account" is correct as the cross-family regression signal and WRONG as evidence for this residual; an observable serving both needs two counters with distinct names, so that nobody later cites the account-keyed number as this exposure's rate | Round 3 finding 2, reproduced at the tip and on `35f156e` | 2026-08-22 |
| TC-023 | `_switchOnSessionReset` ranks among the accounts whose session quota just reset plus the incumbent, and never consults the band, so an available higher-pressure peer that did not reset is simply not a candidate. Reproduced at pressures 6.94e-6 / 2.78e-5 / 3.47e-5: `current` parks on the MIDDLE account while the highest-pressure peer sits available and in band | **Pre-existing**: reproduces identically at the tip and at `e81e0d2`. It violates the **"or passing over"** half of the invariant — *no code path may make an account current if a strictly-higher-pressure account is the one it is leaving or passing over*. The pressure ranking added to that function closes the "leaving" half only, because it ranks the candidate set it is given. Same family as TC-020, different site: both establish `current` from a set that was not chosen by pressure. Closing it means widening the candidate set to the band, which changes when the reset switch fires and wants its own review | Round 3 finding 1, reproduced at the tip and at `e81e0d2` | 2026-08-22 |
| TC-024 | A release presented WITHOUT its hold is authorized by session id alone. `endRequest`'s ownership test fires only when a hold is present, so `endRequest(sid, null)` still decrements `inFlight` and, at zero, runs the drain — the F3 shape returning through the no-hold door: an evicted request ending holdless would drain its live replacement | **Introduced with the per-pin holds in this change, not inherited** — holds did not exist before it. NOT REACHABLE from production, by four links: (1) `server.js:717` is the only production release site; (2) `ctx.hold` is assigned at `server.js:706`, outside and before the `try` opened at 707, so the `finally` always sees the assigned value; (3) `beginSession` returns null only for a falsy `sessionId` (`account-manager.js:695`), and for any truthy id `beginRequest` always returns a hold object; (4) `endSession` short-circuits on a falsy `sessionId` before reaching `endRequest` (`account-manager.js:699`). So `endRequest(sid, null)` with a truthy id is constructible only by driving the tracker directly, which is tests. Deferred rather than closed because the fix costs a full re-verification for a path with no reachable caller. **Tripwire**: a second production release site, or any caller that can reach `endSession` with a truthy id and no hold, makes it live | Round 3 scoped pass, reproduced against the tracker directly | 2026-08-22 |
| TC-025 | `rid` is a per-tracker counter (`this._recordSeq`), so two `SessionTracker` instances both mint `rid=1` and a hold issued by one satisfies the other's ownership test. Reproduced: `A.rid=1 B.rid=1 collide=true`; a foreign end took B from `inFlight=1 holds=1` to `0/0` | **Introduced with the per-pin holds in this change, not inherited.** Two live trackers is not a production shape: one AccountManager, one SessionTracker. **The two boundaries are NOT symmetric, and the one-line fix is wrong.** At the CLAIM boundary (`touch`) adding `s.holds.has(hold)` is free, strictly narrowing and correct by construction — safe whenever that file is next touched. At the END boundary (`endRequest`) requiring membership before decrementing BREAKS lost-hold recovery: a caller that lost its hold would never decrement `inFlight`, the session would never reach zero, and the drain that exists precisely to recover a lost hold would never fire. That trades a real leak for closing a path with no reachable caller. Anyone applying "just add the membership check" symmetrically introduces a leak while fixing nothing reachable | Round 3 scoped pass, cross-tracker collision reproduced | 2026-08-22 |
| TC-026 | The `status` Models row has never consulted route restrictions. An exclusive route (`{match:['*fable*'], accounts:['b']}`) makes account `a` ineligible for Fable — `_routeAllows(a, 'claude-fable-5')` is false and `previewRouteIndex` agrees — while the rendered row still prints `Fable ✓`. Reproduced with `scratchpad/f23-probe.mjs` on quota nowhere near any cap, so the weekly gate is not involved. **A second fixture makes one payload contradict itself**: with no configured route and a `models: ['claude-fable-5']` ownership claim on `b`, `getRoutes()` emits `{ name: 'a', eligible: false }` for the auto-created `fable` route while the Models row of that same payload prints `Fable ✓` (`scratchpad/r3-routes-carry.mjs`) | **Pre-existing, and measured as such**: occurrences of `_routeAllows` in `src/status-renderer.js` are **0 at `b7361d1` and 0 at head** — the renderer has never known about routes, before or after the weekly-gate change. Not newly claimed fixed either: the sentence added to `docs/routing.md` in that change is scoped to quota and says in as many words that a `routes` pin or blocklist can still split eligibility. Not fixed there because the round's subject was the quota gate, and widening it to route awareness would have put an unreviewed second behaviour in a commit whose evidence was built for the first. **What a fix needs, corrected**: the first version of this entry said the payload does not carry route eligibility at all, so a fix is a wire change. Measured on head, it carries the fact two ways and neither is usable. A CONFIGURED route filters an ineligible account out of `routes[].accounts` entirely (`_routeAccountsView`, defined `account-manager.js:2346` and called at `:1636`, both at `20ec1cf`; an earlier draft cited `:1513-1518`, which drifted when this cycle added lines above it — anchors are pinned to a sha from here on), so the fact arrives as absence. An AUTO-created route maps every account with `eligible: _isAvailable(a, sample)` (`:1491`) — one boolean over seven branches (disabled, throttled, exhausted, error, near-quota, route/ownership, advisor-ineligible), so it cannot say *route*. `routing[]` does not close it either: `_bandedCandidates` filters through `_isAvailable` before the band runs (`:1146-1149`), so a route-ineligible account never enters a ladder, and absence from a ladder has those same seven causes. What closes it is per-account, per-family eligibility carried WITH its reason — the shape of `band.excluded[]` (status-interface brief §7, field 5, whose codes include `route-excluded`) — plus `modelRoutingLine` rewired to read it. Phase 1 adds that field to the new Routing block and does not touch the Models row, so this entry survives phase 1 rather than being closed by it. **Checked against what shipped, since the sentence above was written before the code existed** (`scratchpad/tc026-reread.mjs`, `scratchpad/tc026-boundary.mjs` at `6501126`): it is true. `routing[]`'s `fable-only` entry carries `excluded: [a:route-excluded]` and the Models row for `a` still prints `Fable ✓` in the same rendering. Two things the forecast did not say, both measured. **The contradiction is now visible on one screen rather than split between payload and display**: where the route leaves two or more eligible accounts, the Decision block renders that scope and prints `Skipped  route-excluded  a` about eleven lines above the `Models  Opus ✓  Fable ✓` row for the same account. And **where the route leaves exactly ONE eligible account it is on screen nowhere**: removing `a` leaves a single candidate, `decideBand` returns `passthrough`/`single-candidate` for that scope, the block prefers a scope that decided something, and the only trace is `Other scopes  fable-only: passthrough` — a variant name where the fact is "one account can serve Fable". That last line is a caption gap in the block this round shipped, not a pre-existing one. **Scheduled: fixed in round 4**, which team-lead has scoped to carry `--explain` and `--json`; the fix is one line, rendering `selectionSummary(entry)` instead of the bare variant name for a passthrough entry on the `Other scopes` row, which would print `fable-only: one eligible account; nothing to choose between` using a function this round already tests. Named rather than left as "deferred" because a residual with no scheduled owner reads identically to a scheduled one on the day it is written and completely differently a month later, when the only question anyone asks is whose it is | Round 2 scoped cross-model pass, finding 2; provenance measured on both trees; wire claim corrected in round 3 with `scratchpad/r3-routes-carry.mjs` | 2026-08-23 |
| TC-027 | The TUI's `⊘ blocked` tag derives from quota buckets alone, so a family disabled by `config.blockedModels` gets no tag and its route marker still renders live. The same shape as TC-026 in the second renderer: a display deriving eligibility independently of the thing that decides it | **Pre-existing.** `src/tui.js` builds that tag from `q.unified7dSonnet` / `q.unified7dFable` and now from `gatingUtilization`, none of which consults the blocklist. Reported by the round 2 scoped pass by mechanism rather than by reproduction, which is why it was stated as the narrower claim. **Now reproduced**: driving the real `_renderAcct` on a prototype instance, `blockedModels: ['*fable*']` with Fable quota at 0.1 renders no tag, while the control — Fable at 1.0 on quota with nothing blocked — renders `⊘ Fable`, so the tag site is reached in that configuration and the absence means something (`scratchpad/r3-tui-blocklist.mjs`). Still not a live terminal: it exercises the tag's derivation, not the app. The enumeration in round 2 found exactly two displays deriving per-family availability independently of the gate; both were routed through `gatingUtilization` for the QUOTA question, and this is the residue: the blocklist question was never asked by either. **The closing claim is corrected**: this entry said a fix wants the wire field TC-026 needs, so the two close together or not at all. That is false. The blocklist is not a per-account property — `server.js:668` answers a blocked model with a 400 before any account is selected — so no per-account field can carry it, and none is needed. `_renderAcct` already holds `this.config.blockedModels` (the object read at `tui.js:513`, `:1650` and `:1671` at `20ec1cf` — an earlier draft cited `:473` and `:1590`, and `:473` is now the Quota-probe settings row, whose body reads `this.config.quotaProbeSeconds`. A citation that lands on a DIFFERENT live field is worse than one that lands on nothing, because it reads as checked) at the site that builds the tag (`:1305-1312`), and the status renderer's Models row already asks exactly that question there (`blockedState(blocked, { models: [familyModel(label)], globs: ['*<family>*'] })`, `status-renderer.js:140`, `:731`, `:913` and `:1080` at `20ec1cf` — an earlier draft cited `:646`, which is a comment line). **Recipe rewritten in round 3**: it previously named `findFamilyBlock`, which that round deleted when one blocked-classification rule replaced three — a recipe naming a function that no longer exists reads as actionable and is not, which is the same failure class as a stale mutation anchor. The fix is renderer-local and closes independently of TC-026 | Round 2 scoped cross-model pass, finding 3; reproduced in round 3 with `scratchpad/r3-tui-blocklist.mjs` | 2026-08-23 |
| TC-028 | The banded caption is held by two PINS and by no reading. `tools/verify-caption.mjs` refuses to run when the sentence changes (BANDED_PINNED) and `decision-block.test.js` asserts the same literal, so a lying caption cannot ship: mutating `everything within the tolerance ratio` to `NOTHING within` makes the gate exit 2 and 10 tests fail. But both bindings are IDENTITY pins on one string. The gate's banded section replays the ratio rule and grades the admitted set; it cannot read the sentence, and its own refusal text says so. Reword the sentence and update both pins together and nothing grades whether the new words still describe the rule | **Accepted as the limit of a string-level gate.** Grading a caption's MEANING is a different tool: the replay already grades the RULE, which is the part that can be executed. What the pins buy is that a reword becomes a deliberate act with a human in it — the refusal prints the rendered and pinned sentences side by side and tells the reader to decide whether the replay still reads the new one. Corrected from a stronger claim relayed in pass 9, that the gate does not bind the sentence at all: that was measured at `322884e`, one commit before the pin landed in `12f6674`. Reproduced BOTH WAYS during round 3 pass 9 — gate exit 2 with the refusal, suite 972/982 **at the tree of that pass, which is not this one**: at `20ec1cf` the suite is 1030/1030 (1015 top-level `test(` calls). The reproduction is not in question; the COUNT rode an unpinned "at the tip" anchor and stopped describing the tree it names. Corrected on the TC-036 precedent from `bee377b`, where "reproduces at the tip" turned out to name the sha carrying the fix — a number attached to a moving anchor is a number that goes false without anyone editing it | Round 3 pass 9, relayed claim corrected by reproduction at the pass-9 tree (not at `20ec1cf`; see the count note in the entry) | 2026-08-25 |
| TC-029 | `globCovers` is CONSERVATIVE on 54 ordered pairs of the enumerated shape space: it answers "not covered" where coverage genuinely holds. All ten patterns are wildcard-on-one-end with an interior literal (`*a*b*` against `*ab*`, `a*b*` against `ab*`), where the pattern requires two occurrences of something the glob guarantees once in a position the decomposition cannot see. Enumerated exhaustively rather than sampled: every shape over {a,b,*} up to length 4, 8649 ordered pairs, true coverage decided by witness search over 511 ids | **Accepted because of its DIRECTION, which is the whole argument.** The two errors are not symmetric: an OVERSTATE reports a live route as blocked and suppresses a scope that carries traffic; an UNDERSTATE reports a covered route as live, which shows a scope that is merely redundant. Overstates are now ZERO across that space and asserted so. Closing the understates needs real glob-language inclusion — deciding whether one glob's language contains another's — which is a round-4 item bundled with `modelGlobOverlaps` and the version-suffix scope, since all three want the same primitive. The set is frozen PAIR BY PAIR in `test/glob-coverage-oracle.test.js`, so a new conservative answer fails the suite even though the existing 54 do not | Round 3 pass 10, verifier's oracle; guard landed as the invariant | 2026-08-25 |
| TC-030 | `familySplit` warns on 56 entry/fleet combinations that cannot be divided, and misses NONE (verifier's oracle v3 at `9107ab0`; the v2 figure of 101 that this round's commit messages cite is inflated — v2's quota arrangements topped out at 0.95, under the 0.98 switch threshold, so it could never construct an id with NO destination and 45 of those rows can in fact diverge). Ground truth is direct simulation, not a second copy of the rule: 84 route configurations x 4 claim shapes, 872 entries, every covered id routed for real under arrangements that cross the threshold, disclosure required iff any id lands somewhere other than the representative's account | **Accepted, and the remainder is THREE KINDS rather than one — the fix was attempted and measured, not declined.** Of the original 210 at `f2359de`: ~89 were claims-measurement noise, closed by the family-pattern, explicit-accounts and one-id work; up to 37 are arguably CORRECT under the field's own contract, where ownership diverges even though today's destinations coincide — the oracle's ground truth was narrower than the field it graded, which is an instrument limit and is recorded as one; and ~60 are entries an earlier route has taken ENTIRELY, which are DEAD rather than split. "Split" is the wrong question for those, not a wrong answer, and they need a THIRD DISCLOSURE STATE rather than a better binary. **The two cheap fixes do not work and here is why**: an id census cannot decide "took the whole family", because the only census available is `FAMILY_MODELS` plus ids named literally in routes and claims — with `claude-fable-5` exact ahead of `*fable*` it holds one Fable id, all of it is taken, and the rule would call the entry dead and go SILENT on a genuine split; and narrowing claims to the ids a scope still carries closed zero of its four rows for the same reason and was reverted rather than shipped. The third state's predicate must therefore be GLOB-LANGUAGE (`globCovers(earlierGlob, thisGlob)`) — decidable, sound, never false-dead — which fires seldom and never lies, rather than a census claim that fires often and sometimes lies. Round 4b, with the glob-inclusion primitive TC-029 needs | Round 3 pass 10, verifier's familySplit oracle v2 and v3 (`probes/familysplit-oracle2.mjs`, `familysplit-oracle3.mjs`, `logs/familysplit-deadvssplit.json`); three roots fixed, the remainder measured and deferred | 2026-08-25 |
| TC-031 | The per-account figure suppression is SUFFICIENT, not exact: it withholds figures when a captured entry's numbers CAN be wrong for the traffic its route receives, and does not decide whether they ARE, so some true figures are withheld. Asked as a union, because each half covers what the other cannot — (a) some account is barred from the REPRESENTATIVE and the route's glob reaches past the ids config names, or (b) a claim discriminates on an id the route actually RECEIVES | **Accepted on DIRECTION. THREE claims this entry once made are refuted by execution and retracted here rather than quietly dropped.** *0 under-suppressed* — FALSE. The predicate asked its ownership question about the CAPTURED REPRESENTATIVE, the one id the route provably does not receive, so a fleet claiming a SIBLING published an inversion (`probes/r3-pass13-p1.mjs`, 2 of 2, control live). *0 over-suppressed* — ALSO FALSE, and live TODAY with a reproduced witness: one account claiming both ids and the other neither, both questions admitting the same single account which really does serve the traffic, figures TRUE, withheld anyway (`probes/r3-oversuppression.mjs`, 1 of 1, control live). Ruled the tolerable direction: a withheld figure carries a stated reason, a wrong one is acted on. Known NON-EMPTY with a concrete case, and still unquantified — sizing it needs the census TC-030 records as measured-unsafe. *The census is complete, so the boundary is EMPTY* — the worst of the three, because it asserted an ABSENCE that the shipped defect then falsified. **ITS ORIGIN IS RECORDED DELIBERATELY: the claim was the VERIFIER'S, relayed as a finding, and this entry's author took it in good faith and pinned it with a test.** It was one step wider than the measurement under it, and the author who inherited it did not re-derive it before writing it down. Recorded this way because a residual is the round's memory: an entry that credits its author with every claim inside it leaves the next reader unable to tell a defect in the CODE from a reviewer's confident SENTENCE. This one was the latter, and both halves of that — the confident sentence and the unchecked inheritance — are how it shipped. **Completeness has to name its question:** the config census IS complete for *can ownership discriminate on id X* — the ids where `allowed(id)` is anything but ALL ACCOUNTS are exactly the claim strings plus their `[Nm]` strips, pinned by a test that reddens if `_accountOwnsModel` ever starts glob-matching. It is NOT complete for *do allowed(representative) and allowed(received) name the same accounts*, which is the question the FIGURES need: an id no claim names admits EVERYONE, so a restricted representative differs from an unnamed received id, and the unnamed ids are unbounded. **THE GAP IS REAL AND IT IS COVERED — which is a stronger statement than either 'empty' or 'we cannot say'.** Disjunct (a) closes it by firing on PRESENCE (a restricted representative plus a wildcard glob) and enumerating nothing at all; disjunct (b) is complete for per-id discrimination. Neither half claims completeness for the whole question; together they are sufficient, which is the property that matters. The id that proved this (`claude-fable-4`, named by nothing and absent from `FAMILY_MODELS`) is exactly why a WIDER census would not have helped and the presence-based half was needed | Round 3 part (b) revision and the pass-13/14 corrections; over-broad predicate at `1b0c64f` (`r3-successor/probes/discriminates.mjs`); under-suppression, over-suppression and the trade-not-add dual by the verifier (`r3-pass13-p1.mjs`, `r3-oversuppression.mjs`, `r3-pass14-p1.mjs`); the trade-not-add regression independently caught by `r3-successor/probes/false-exclusion.mjs` | 2026-08-25 |
| TC-032 | `decidePick`'s comparison is NON-TRANSITIVE on a MIXED fleet, and the winner therefore depends on the order candidates arrive in. The `load` term is pairwise-conditional (`applies: (a, b) => a.observed > 0 && b.observed > 0`) because an unmeasured account is unmeasured rather than idle — so on a fleet where some accounts are measured and some are not, a term applies to some pairs and not others, which is not a total order. Three accounts can form a cycle and all three can win: `pair(a,b)=a`, `pair(b,c)=b`, `pair(a,c)=c`, and all six orderings of the three produce three distinct winners | **Accepted on its bounds, and ROUND-INTRODUCED IN THIS TREE. This entry shipped saying the opposite; the correction is kept in place rather than tidied away, because the way the false claim got in is worth more than the claim.** Bounded twice, and both bounds are asserted rather than described: it is confined to MIXED fleets — with every candidate measured, or none, every term applies to every pair and the chain is a total order again — and it affects WHICH of several reasonable accounts is picked, not whether a bad one is. The alternative was dropping the `applies` gate, which is what made an unmeasured account rank first on `load` and sent a session's first turn to the wrong place; that cure is worse. **IT IS NOT A FOLD-BACK GAP, AND THAT CLAIM IS WITHDRAWN.** The entry read "the code rode the fold-back (`decidePick` is ported VERBATIM from `upstream/load-weighted-selection` 530d16b)". Measured at two shas rather than read: the property is ABSENT at the round's base `63e6e93` — one winner across all six orderings — and PRESENT at the tip, three winners, with an all-measured control giving exactly one winner at BOTH shas so the probe is known to be measuring the gate. This round introduced the behaviour here. `530d16b` — the head of the PARKED `upstream/load-weighted-selection` branch, cited cross-branch and NOT REACHABLE from the frozen sha, which is stated so a reader who cannot resolve it knows that is expected rather than a typo — is not an ancestor of this branch (`git merge-base --is-ancestor`: no) and the two `src/pick-decision.js` blobs differ (`a24aa8a` vs `83f3d83`); `git diff 63e6e93..HEAD -- src/pick-decision.js` is where the `applies` gate, its `@type` annotation, and the matching filters in `decidingTerms` and `tiedWith` enter. **THE RULING NONETHELESS TRANSFERS, which is a separate question from the provenance and is why this stays accepted rather than becoming a finding.** The `load` term line is byte-identical to the slice's, so the mechanism the slice ruled on IS this mechanism; both bounds are re-verified here by execution rather than carried. **HOW THE FALSE CLAIM GOT IN:** something narrow and TRUE was widened by one step. `test/pick-decision.test.js` says the FIXTURES were ported verbatim — "`cand`/`snap` are the slice's own helpers" — which is accurate, and is a claim about the TEST FILE. The register turned it into a claim about the SOURCE FUNCTION, and the true half made the whole sentence feel checked. **Every provenance adjective — stock, inherited, ported, pre-existing, fold-back — is a claim and carries its execution evidence: behaviour at the claimed origin, plus ancestry or blob comparison, both by command.** The tests do hold the property at the tip: `KNOWN PROPERTY: the gate makes the comparison non-transitive on a mixed fleet` asserts the cycle by construction, and the randomized minimality test draws its measured/unmeasured regime per fleet so the non-transitivity is not smuggled into a minimality claim. The register was the only place it was missing, so a reviewer reading this file found no mention of transitivity anywhere in it and correctly raised the comparison as fresh — a disclosure that lives only where it was written is one the next reader cannot find | Round 3 pass 15, codex (`pick-decision.js:177`), hypothesised by the verifier as a missing fold-back and entered here on that reading. Provenance CORRECTED in the closing pass by execution at both shas (`r3-finisher/probes/transitivity-provenance.mjs`, control live at each; logs `transprov-base.log`, `transprov-tip.log`), with `test/pick-decision.test.js` 30/30 at the tip including both KNOWN PROPERTY tests | 2026-08-25 |
| TC-033 | `setRoutePin` REJECTS the account a route lists and ACCEPTS one that route cannot use, whenever the route's representative is captured by an earlier route. `_routeSample('wild')` strips `claude-fable-5*` to `claude-fable-5`; an earlier exact route owns that id; and `_routeAllows(account, sample)` is called with NO route, so `_routeForModel` answers for the EARLIER route. Reproduced with a live control (`probes/r3-pass15-pin.mjs`): with `wild` listing `["b"]`, `setRoutePin('wild', b)` returns `ok=false, reason="route \"wild\" does not allow \"b\""` while `setRoutePin('wild', a)` returns `ok=true`; with an UNCAPTURED sample the same fixture accepts b and rejects a, correctly | **PARKED TO ROUND 4a ITEM 1, which fixes it at the root, and disclosed here rather than carried silently — this freeze attests to the tree AS DISCLOSED, and a defect the declaration does not name is an omission rather than a park.** **IT IS A WRITE PATH, and that is the reason it is ranked above every other instance of this defect shape found in the round.** All the others were REPORTS: a wrong number on a screen, which a reader can disbelieve. This one MUTATES — `setRoutePin` is the operator action, and the TUI's switch-mode pinning goes through it — so an operator pinning a route is silently given the wrong outcome: told the account they chose is not allowed, and allowed to pin one the route cannot use. **PRE-EXISTING IN STOCK, verified by execution rather than inherited as a premise:** the defect reproduces IDENTICALLY at base `63e6e93` with the control behaving there too, and `setRoutePin` and `_routeSample` are byte-identical base to tip (`git diff 63e6e93..HEAD -- src/account-manager.js` contains zero `setRoutePin` lines). The round neither introduced it nor touched it. **THE FIX IS THE ONE THIS ROUND HAS APPLIED FOUR TIMES:** pass the stored route as `_routeAllows`'s third argument instead of letting `_routeForModel` answer. It is deliberately NOT applied here because round 4a item 1 fixes the SAMPLE ROOT — `_routeSample` duplicates the wildcard strip inline, so a repair at `sampleModelFor` alone misses it — and 4a opens at this freeze. Same root, same fix, same item: the F7-marker P2 from the same pass — whose machinery is `tui.js:1167-1169` (the `familyTarget` construction, where the hardcoded `previewRouteIndex('claude-fable-5')` sample lives and therefore where the shared root actually sits), `:1293-1294` (`familyMark`) and `:1350` (its F7 emission), all re-derived and read at `106bc66`. **THE `tui.js:1129` THIS ENTRY CITED IS NOW A BARE `//`** — a citation landing on nothing, which is the better of the two failure modes the register bring-up found and still a citation nobody can check. **AND THE RE-DERIVATION NOTE BELOW IS ITSELF STALE, WHICH IS THE PART WORTH SAYING OUT LOUD:** this is the entry that COINED *a stale line number is a citation that quietly points at the wrong code*, and it claimed its numbers were re-derived at the pass-16 fix sha — two fixes ago. **AN ENTRY CLAIMING ITS NUMBERS WERE RE-DERIVED IS WORSE THAN ONE CLAIMING NOTHING**, because the claim is exactly what stops the next reader checking. Every anchor in this entry is now sha-qualified so it resolves at the sha it was read at rather than at whatever the tip becomes. **THE PASS-16 REMAINDER LANDS HERE TOO, and it is the same root read from the other end.** A route whose scope had its figures withheld now names NOBODY and says why — but the account that serves the route's remaining traffic is real and goes unnamed. The progression, measured at three trees, is why that is an improvement rather than a residual defect: base `63e6e93` named `{a, b}`, over-broad but CONTAINING the truth; `a021cfc` named `{a}`, DISJOINT from the truth and stated with confidence, which is the worst of the three; the fix names `{}` with the reason on screen — honest and incomplete. Naming the serving account today would mean publishing figures nobody computed, since the withholding exists precisely because the representative is captured. It closes when this item's sample-root fix computes for the ids the route ACTUALLY RECEIVES. Marked, not gated: `r3-finisher/probes/dupjoin-split.mjs` gates the INVERSION half (a foreign route's owner must not be named — closed by the injective join) and reports the serving-account half as an EXPECTED STATE whose expectation flips when 4a lands. A permanently-red gate is a gate people stop reading. **LINE NUMBERS IN THIS ENTRY AND TC-027 WERE RE-DERIVED at the pass-16 fix sha** — `src/tui.js` gained the route-glyph redirection, so every citation into that file shifted; a stale line number in a register is a citation that quietly points at the wrong code | Round 3 pass 15, codex and grok independently (`account-manager.js:2272`); reproduced by the verifier and again by the author at both tip and base | 2026-08-25 |
| TC-034 | `decidePick`'s `decision.by` is derived from the FIELD, not from the CONTEST, so it can name a term that separated the winner from an also-ran while the finalists were settled by a later one. `const [by = 'first'] = decidingTerms(...)` takes the first term on which the winner differs from ANY candidate. With the winner and the runner-up tied on priority and a third account in a worse priority tier, it reports `by: 'priority'` for a contest priority took no part in | **PARKED to round 4b as a FOLD-BACK item, and STOCK — the round neither introduced nor touched it.** Two bounds, both measured rather than described. **(1) STOCK, BY EXECUTION:** the reviewed slice's own F3 fixture (`upstream/load-weighted-selection` 530d16b, `test/pick-decision.test.js`, "`by` names the term that separated the finalists, not the field") runs here with the SAME PICK the slice asserts (`index 1`) but reports `by: "priority"` where the slice asserts `"load"` with the message "priority took no part in the contest it won" — and the output is IDENTICAL at base `63e6e93` and at the tip, control live at both (`r3-finisher/probes/by-from-the-field.mjs`). **(2) THE ROUND'S OWN CHANGE DOES NOT WORSEN IT, also by execution, and this bound was left open once and then closed rather than carried as a hedge:** this round added an `applies` filter to `decidingTerms`, which is the function `by` reads, so the question was whether that could make `by` worse on a MIXED fleet. Measured (`r3-finisher/probes/by-mixed-fleet.mjs`): on a mixed fleet the round's filter REMOVES an inapplicable `load` — base reports `by: "load"` with terms `["load","sessions"]`, the tip reports `by: "sessions"` with `["sessions"]` — so the round REFINES this path. The mechanism agrees: the tip's filter is strictly narrower, so its deciding-terms set is a subset of the base's and it cannot introduce a term that does not differ. **IT IS A REPORTING FIELD.** The account picked is unaffected — the pick matches the slice's on the slice's own fixture — so the cost is an explanation naming the wrong term, not a wrong destination. **THE FIX ALREADY EXISTS UPSTREAM and that is what makes this a fold-back rather than a design question:** the slice derives `by` from the winner-vs-RUNNER-UP contest (`separatingTerm(best, rest[winnerIndexOf(rest)])`) and keeps `decidingTerms` as the separate FIELD question, with a test for each. The campaign tree kept stock's shape. Round 4b, beside the other fold-back items. **STILL OPEN, stated because it is not covered by either bound above:** the field-vs-contest gap is independent of the `applies` filter and is live at BOTH shas; I have not enumerated which other fleet shapes surface it | Round 3 closing pass, found while checking TC-032's provenance claim against the slice — the slice's rejected-alternative comment names this exact failure, which is how the tree was asked the question. Reproduced at tip and base with a live control, and on the slice's own reviewed fixture | 2026-08-25 |

| TC-035 | A PARTIALLY blocked route names the accounts admitted for its BLOCKED representative, and the account serving the live remainder goes unnamed. `routeNaming` drops a scope only when `blockedState` answers `blocked`; a `partial` scope survives, and its `band.admitted` was computed for `e.model` — the one id the scope was graded on. Where the representative is the blocked id and the glob still carries live siblings, the surviving scope contributes accounts chosen for traffic that cannot flow. Reproduced with a live control (`probes/p19-partialblock.mjs`): `*opus*` with `claude-opus-4-5` blocked renders `→ repOwner …` while `claude-opus-4-1` is live and served by `sibOwner` | **ADVISORY under the round's pre-registered boundary, and the grade is MEASURED rather than argued.** Clause (a) is the CONJUNCTION *excludes a known server AND appears complete*, and the second half is false: the line carries `(partly blocked)`, and since `9481a7b` also `(split by model claims; other ids may go elsewhere)`. **THE PROVENANCE SENTENCE THIS ENTRY WAS HANDED WAS WRONG, AND IS CORRECTED BY EXECUTION RATHER THAN QUIETLY DROPPED.** It read *the pass-18 fix is what downgraded it — this would be blocking on yesterday's tree*. Measured at six trees with a live control at each: `(partly blocked)` is already present at `6883e25` and at every tree after it, so it PREDATES the exclusion rather than repairing it; the `basisGap` qualifier arrived one cycle earlier than the sentence said, at `9481a7b` (pass 17), not `209a673` (pass 18); and **there is no tree in this round at which this fixture both excluded the server and appeared complete**, so the counterfactual is false at every yesterday available. **WHAT IS ROUND-INTRODUCED IS THE EXCLUSION ITSELF, which is the fact worth recording.** At `6883e25` the line named `repOwner sibOwner` — over-broad but CONTAINING the truth; from `7fe4429` it names `repOwner` alone. `band.admitted` is `["repOwner"]` at BOTH shas, so the payload did not change: what changed is which field the renderer consults, which is precisely what `7fe4429` set out to do. The narrowing is the round's own deliberate trade — it replaced a path that, on this fixture, did not consult the band: the rendered names equal `routes[].accounts` while `band.admitted` holds one of them. Scoped to the fixture that measured it, because one fleet cannot support a claim about what that path did in general. **The SOURCE is measured; the FILTER is not, and the entry stops short of claiming it.** At `6883e25` the rendered names equal `routes[].accounts` exactly and `band.admitted` holds one of them, so the second name came from the route's wire view rather than the band. Whether that view was eligibility-filtered is NOT decidable on this fleet, where both accounts are eligible and the filtered and unfiltered sets are identical (`probes/p19-735-mechanism.mjs` prints both and says so). An earlier draft asserted the filter — and it is disclosed rather than silent. It is nonetheless a true name the previous tree had and this one does not, so the transition is recorded by sha instead of summarised. Closes when a scope is graded on the ids the route RECEIVES rather than on one representative — the same sample root as TC-033 and TC-038, round 4a item 1 | Round 3 pass 19, codex (`status-renderer.js:730`); reproduced with `r3-finisher/probes/p19-partialblock.mjs`; provenance measured across six trees with `r3-finisher/p19-730-provenance.py` (log `logs/p19-730-provenance.log`) | 2026-08-26 |
| TC-036 | `band.admitted` and `band.excluded` are BOTH keyed by account NAME on the wire, and names are not unique. The renderer must union admissions across scopes and subtract accounts a scope cannot serve; both sides of that arithmetic arrive as strings, so excluding one account erased its admitted namesake and a route whose admitted account was real rendered `→ (none)` while that account served it. Measured at three shas, and the BASE's result is recorded as what it is rather than as a clean pass: at `63e6e93` the fixture cannot CONSTRUCT the collision at all — both duplicates land in `band.admitted`, `band.excluded` is EMPTY, the probe reports PREMISE ABSENT, and the line renders `-> dup dup`. The exclusion machinery this round added is what makes the collision possible. The erasure reproduces at `9481a7b` and at `209a673`, the tree this cycle opened on, and is CLOSED at the shipped sha — an earlier draft wrote that as "at the tip", which reads false, because the tip is precisely where the fix is. **AN EARLIER DRAFT SAID THE BASE NAMED BOTH "permissively, from the configured list"; that mechanism was never measured and it is false** — those names came from `band.admitted`, which held both. Corrected rather than quietly dropped | **STATED AS A LIMITATION WITH A CONSEQUENCE, AND DELIBERATELY NOT AS A SCHEDULED FIX** — nobody in this campaign owns upstream's roadmap, and "scheduled" would put a commitment on it that nobody here can make. If it becomes an upstream conversation that is a post-freeze decision, and this entry is the artifact it would cite. **THE RENDERER COMPENSATES, AND THE COMPENSATION IS SOUND WITHOUT BEING COMPLETE: ADMISSION WINS THE COLLISION.** If a name is admitted at all then at least one account bearing it can serve, so naming it is the true statement and subtracting first made the false one. What no renderer rule can recover is WHICH bearer: a fleet with two accounts of one name, one admitted and one excluded, gets a single name on screen and no way to tell them apart. **THE PROPER REPAIR IS UPSTREAM AND IT IS NOT A RENDERER CHANGE.** Stable per-account indices exist and the band is built from indexed accounts; the wire simply does not carry them into `band.admitted` / `band.excluded`. **Cited rather than asserted** (`account-manager.js:1943` at `20ec1cf`; an earlier draft cited `:1902-1918`, and THIS IS THE ENTRY WHOSE OWN FRAMING IS "cited rather than asserted" — the citation was real when written and rotted two commits later, which is the argument for pinning rather than for citing less): the same object literal that emits `admitted: banded.map(a => a.name)` and `excluded: ... v.account.name` also emits its ladder rows as `account: nameOf(row.account.index)` — the index is in hand at the moment of conversion, on the lines beside the two fields that discard it. Carrying the index — or any stable identity — beside the name closes it exactly. Nothing the renderer can do closes it, because it never had an identity to discard. **ONE MECHANISM, NOT TWO, and the first repair was withdrawn for being UNTESTABLE rather than for being wrong.** It filtered the exclusion set AND made the admitted loop skip the check; each change alone prevented the defect, so no single-line mutation could restore it and the neutralisation row SURVIVED whichever line it targeted. **Redundancy is what a row cannot kill** — belt-and-braces reads as caution and defeats the only instrument that could vouch for the fix. The rule now lives in one place and the row dies on its own test | Round 3 pass 19, codex (`status-renderer.js:761`); run at `209a673`, `9481a7b` and `63e6e93` with `r3-finisher/probes/p19-dupnames.mjs` — REPRODUCED at the first two, PREMISE ABSENT at the base, and NOT REPRODUCED once the fix landed; the base's result is not a pass and is not quoted as one; held by `an admitted account is not erased by a different account sharing its name` plus a matrix row red-verified against it | 2026-08-26 |
| TC-037 | The `familySplit` EARLIER-ROUTE arm asks `modelGlobOverlaps`, which is documented advisory and not decidable in general, so an earlier route can divide a family invisibly. `reaches = g => modelGlobOverlaps(g, scopeGlob) && !globCovers(g, scopeGlob)` (`account-manager.js:2254` at `20ec1cf`; an earlier draft cited `:2213`). **Witnessed instance, reproduced here rather than quoted**: an earlier `claude-*-4` route (accounts `[a]`) ahead of `*fable*` (accounts `[b]`), no `models` claims anywhere. `modelGlobOverlaps('claude-*-4','*fable*')` is FALSE though `claude-fable-4` matches both, `globCovers` is false, `familySplit` is null, and the line renders `*fable* → b` with no marker — while `claude-fable-4` really resolves to `a` and `claude-fable-5` to `b`. The exact-glob control on the same fleet (`claude-fable-4` as the earlier route) DOES fire `an earlier route`, so the arm is reached and the PREDICATE is what misses | **A WITNESSED INSTANCE OF A BOUND THIS ROUND ALREADY DECLARED, NOT A NEW DEFECT — recorded because a bound with no witness reads as theoretical.** `209a673` split this predicate deliberately and certified only the CLAIMS arm; the code comment at `:2239` names `model.js:156` as the licence for leaving the earlier-route arm on the heuristic. The miss is TC-029's conservative direction at a second site: `modelGlobOverlaps` UNDERSTATES overlap when both patterns carry an interior wildcard, and an understate here LOSES a disclosure rather than inventing one. **THE UNCOMFORTABLE PART, STATED PLAINLY BECAUSE A READER DESERVES IT: in this instance the line renders the COMPLETE form.** `status-renderer.js:795` says a route whose measured basis is a proper subset "may not present its names as the whole answer", and here it does — the same *excludes a known server while appearing complete* shape that comment exists to prevent. That claim is therefore BOUNDED in place rather than left one step wider than its evidence, which is TC-031's failure and is not repeated: the marker is exact for the claims arm and advisory for the earlier-route arm. It is not blocking under the boundary because it is neither round-introduced (`familySplit` is ABSENT at `63e6e93` by `git grep`, so the base had no family-split disclosure for this to be a regression FROM — narrowed from an earlier draft's "no disclosure mechanism at all", which overreached: the base does render a Routing block and does qualify a route it considers blocked) nor a contradiction of the round's own claims (the arm was declared advisory in the commit that shipped it). **THE BOUND IS INVISIBLE AT THE POINT OF READING, and without this sentence the register would record a limit the product does not express.** Checked in the code rather than answered from memory: `basisGap` is `familySplit` off the entries and the marker renders only when it is truthy, so when the advisory arm MISSES, `familySplit` is null, `basisGap` is null, and the line renders THE COMPLETE FORM. There is no third state. The identical line is therefore shown for (a) genuinely complete, no split exists, and (b) a split exists and the advisory arm could not detect it — **and an operator cannot tell them apart.** The limit lives in the commit body and in this entry; the screen carries no trace of it. This is the round's own principle that a disclosure cannot buy its correctness by narrowing what it claims, because the narrowing is invisible where it is read. It does not make the finding blocking and it is NOT a request for a fix — making the line distinguish the two states means the renderer deciding what the advisory arm could not, which is the class this round has refused all cycle. The honest move is to say so here, so the next reader does not take a bare line for "measured complete". Claims on the remaining ids CAN mask it, and the fixture is claim-free for that reason — stated as the one observed instance rather than as a rate, because no rate was measured. The instance: a test written for this arm during this cycle passed for the wrong reason until its fleet's `models` claims were removed, because the live scope reported its own `familySplit` and the line qualified itself whatever the earlier-route arm did. **"Often" was in an earlier draft of this sentence and is withdrawn as an unmeasured quantifier**, which is the same rule this round applied to "every other instance in the round". Closes with the glob-language inclusion primitive TC-029 and TC-030 both need, round 4b | Round 3 pass 19, grok; reproduced with a live control by the author before entry (`r3-finisher/probes/p19-earlierroute-advisory.mjs`, exit 1) | 2026-08-26 |
| TC-038 | `_scopeModelsFor` SYNTHESISES a representative for any glob that names no metered family, and every downstream question is then asked about an id nothing can request. A glob matching no `FAMILY_MODELS` representative falls through to `glob.replace(/\*/g,'') \|\| 'model'`, so `claude-*-4` publishes an entry whose `model` is `claude--4`. That string names no family, so it meters the SHARED weekly bucket, while the real id this fixture exercises (`claude-fable-4`) meters the Fable bucket — stated as the id measured rather than as a claim about every id the glob can carry, which was not enumerated; and no `models` claim can discriminate on it, so ownership admits everyone. Measured: with `fourOwner` claiming `claude-fable-4` and `fiveOwner` claiming `claude-fable-5`, the entry admits BOTH while `claude-fable-4` is `fourOwner`-only | **RULED BLOCKING ON CLAUSE (c) OF THE PRE-REGISTERED BOUNDARY, AND FIXED IN THE COMMIT THAT CARRIES THIS ENTRY — the disclosure, not the computation.** The chain, which is precedent rather than preference: the four-form table is ROUND-AUTHORED and freeze-graded, it says names come from a MEASURED basis, and this line rendered a SYNTHESISED basis in the measured-complete form with no qualifier. That is FALSE-MEASUREDNESS — the same family as false-completeness, one level down — and the honest forms this round is built on are load-bearing only if a line that appears measured IS measured. A line may be over-broad WHEN IT SAYS SO, or when no measurement is claimed at all; a silent synthetic basis is the line claiming a measurement that never happened. **WHEN THE FALSIFIED CLAIM IS ROUND-AUTHORED, THE CLAIM'S BIRTH DATE IS THE PROVENANCE** — the base rendered the same pixels and CLAIMED NOTHING about them, so there was nothing there to falsify. **THE FIX IS DISCLOSURE AND DELIBERATELY NOT THE SAMPLE ROOT**, which stays round 4a item 1 and is strengthened rather than preponed by this witness: the PRODUCER marks a scope whose representative IS NOT AN ID THE ROUTE CAN RECEIVE (`basisSynthetic: 'unmetered-glob'`, set on BOTH entry paths so the two cannot drift), and the renderer's whole-entry-set gap aggregation carries it to the line. **THE SENTENCE THIS ENTRY ORIGINALLY QUOTED — `(no id this route receives was measured)` — IS WITHDRAWN**, and is kept as what the line said at the sha this entry describes rather than as a description of the tree. Ruling B narrowed it to `(basis derived from the glob, not matched to a metered family)` because the old form claimed NON-EXISTENCE, a fact about the world no predicate here can decide; see TC-045. **THIS PARAGRAPH ASSERTED THE WITHDRAWN FORM FOR A FULL CYCLE AFTER B LANDED**, while TC-045 four entries below diagnosed exactly that claim and neither entry named the other — the THIRD instance of A CORRECTION ENTERED AS A NEW ENTRY DOES NOT REPAIR THE OLD ONE, and the reason a withdrawal now triggers a CENSUS over living artifacts instead of a correction written somewhere else. **THE DECIDABLE TEST IS THE WILDCARD STRIP, NOT `FAMILY_MODELS` MEMBERSHIP** — `account-manager.js:1904-1905` at `2e1ff12` (the `derivedBasis` binding — NAMED because a bare line+sha citation fails silently on the next edit above it, which is exactly how this citation broke: the lines were advanced to the new sha and the label was not, leaving it pointing at a scope object literal. At `da05c45` the same binding is at :1895) is where this predicate lives now; the historical citation below is kept because it is what the entry measured. `account-manager.js:1867` at `8b1c368` reads `scopeGlob && model === stripped && stripped !== scopeGlob`: the representative is the strip's output AND the strip altered the glob. **THIS CAUSE LINE ASSERTED THE MEMBERSHIP TEST UNTIL 2026-08-26 AND THE TREE STOPPED USING IT AT `e51b405`** (*routing: the mark asks whether the basis is an id, not whether it is metered*), which over-qualified every wildcard-free route on a real-but-unmetered id. That defect and its provenance are TC-040. **THE REPAIR IS RECORDED RATHER THAN MADE SILENTLY, BECAUSE THE WAY IT SURVIVED IS A CLASS: A CORRECTION ENTERED AS A NEW ENTRY DOES NOT REPAIR THE OLD ONE.** TC-040 was written four entries below this one, diagnosing this exact mechanism form as the wrong shape and stating the invariant that replaces it, and this line went on asserting it — so the register held the diagnosis and the disease four entries apart. It then recurred one commit later: the amend that enriched TC-040 with the full misquote chain left this line untouched again. The correcting entry attracts the attention; the rotted one sits still. Found by r3-attestor2's census BY ENTRY rather than by count — the raw phrase count is 2 before and after that amend, so a count could not tell the two situations apart, and this author's own check missed it by grepping the phrase WITHOUT the backticks the register carries. A census grepped by spelling is a guess wearing one. It gets its OWN sentence rather than joining `basisGap`, because `basisGap` renders as "split by X; other ids may go elsewhere" — a claim that the figures are REAL and merely partial, which on a placeholder basis they are not. A qualifier that misdescribes what it discloses is worse than a bare line. Naming who really serves those ids would mean the renderer deciding what the producer could not measure, which is the display-derives-eligibility class this round has found seven times. **WHAT REMAINS A RESIDUAL** is the figures themselves: still computed for a placeholder, now stated to be. **THE DIRECTION BOUND BELOW IS THE SEVERITY BOUND, NOT A DEFENCE** — it argues for the SIZE of the fix, and it is why this was one small disclosure rather than an emergency. Both substitutions err the same way. OWNERSHIP: an id no claim names admits everyone, so the named set is a SUPERSET of the truth. QUOTA: `gatingSource` (`model.js:492`) returns max(shared, family) for a family bucket and shared alone for the shared one, so the synthesised id gates on a value never larger than a real id's — it can admit an account the route's ids gate, and can never gate one they admit. Both halves therefore land on OVER-BROAD CONTAINING THE TRUTH, the direction this round already accepts, **and which the BASE ALREADY PRODUCES — measured at both trees rather than inferred from either direction.** On the same fixture, `63e6e93` and the shipped sha render the line BYTE-IDENTICALLY: representative `claude--4`, admitted `[healthy, fableSpent]`, `claude-*-4 -> healthy fableSpent`, silent, with the true server named in both. So the behaviour is PRE-EXISTING, there is no regression, and the boundary's round-introduced trigger does not fire. **TWO WRONG SENTENCES ARE BURIED HERE AND BOTH ARE KEPT VISIBLE, because the way they got in is worth more than either.** The FIRST draft said the base "already had" this direction — true, but asserted rather than measured. The CORRECTION then said the opposite, that it is ROUND-INTRODUCED, on the grounds that `_scopeModelsFor` and `familyModelsMatching` are absent at `63e6e93`. Both those functions ARE absent; the base reaches the same synthesised id through stock's own wildcard strip, so `_scopeModelsFor` is a NEW CONSUMER of an OLD strip. **The presence of a FUNCTION was measured and reported as the provenance of a BEHAVIOUR** — two different questions, one of them asked. What made it possible is that the probe imports a round-introduced helper and therefore CRASHES against the base: a crashing probe was read as evidence instead of as a reason to write one the base could run (`probes/p19-producer-base.mjs`, which imports only what both trees have). "The setup would not run" is not "the hypothesis is disproved". **Measured rather than reasoned from the source**: a witness with a live control renders `claude-*-4 → healthy fableSpent` where `fableSpent` is gated for the Fable id the fixture exercises while the control on the same fleet renders `*fable* → healthy` — **"gated for EVERY real id the route carries" stood here through two correction passes and is false**: `fableSpent` is spent on the FABLE bucket and healthy on the shared one, and `claude-*-4` matches non-Fable ids just as readily. Its twin four lines above was corrected while this one was read straight over, twice; the mechanical audit found it on the third pass; and the implication *gated on the synthesised id ⇒ gated on a real id* holds on 81 of 81 enumerated quota pairs through the shipped function. **AN ARRANGEMENT WAS DISCARDED RATHER THAN REPORTED, and the probe records why**: a first fixture gated the serving account by pushing it past the SHARED bucket, which gates every model, so the control rendered `(none)` too and the disjoint result it produced was attributable to the fixture rather than to the substitution. A dead control is not a finding. **THE ENUMERATION THIS ROUND SHIPS IS CONSUMERS-ONLY**: it certifies that each site reads its declared source and says nothing about whether the producer put a true fact there. This entry is that gap named at the one site that matters, and `_scopeModelsFor` is stated as the enumeration's precondition. Same literal strip as stock's `sampleModelFor` and the same root as TC-033 — `_scopeModelsFor` is a NEW consumer of it on the measured-basis path. Closes at the sample root, round 4a item 1 | Round 3 pass 19, grok; inspected BY FIXTURE on team-lead's ruling with `r3-finisher/probes/p19-producer-fakeid.mjs` (ownership half) and `p19-producer-bucket.mjs` (bucket half, witness + 81-pair bound) | 2026-08-26 |

| TC-039 | The F7 / S7 family mark names ONE account from a hardcoded representative id, beside a bar that meters a bucket every account shares. `familyTarget` is built from `previewRouteIndex('claude-fable-5')` and `previewRouteIndex('claude-sonnet-4-6')` (`tui.js:1167-1169`) and `familyMark` marks the single account whose index matches. But the BAR is a weekly BUCKET meter and the MARK is a per-ID routing claim, and routing is per id: with an earlier `claude-fable-4` route to `alpha` and `*fable*` to `beta`, `previewRouteIndex` disagrees with itself across the family (0 for `claude-fable-4`, 1 for `claude-fable-5`), the mark lands on `beta` alone, and `alpha` serves Fable traffic with a blank cell | **PRE-EXISTING, BY EXECUTION** — the same fixture renders identically at the round base `63e6e93`, mark on `beta`, blank on `alpha` — so it is advisory under the boundary rather than round-introduced, and the round neither introduced nor touched it. **THE DESIGN POSITION IS REAL AND IT IS WHERE THE BOUND SITS.** The source comment argues the semantic deliberately: "every account meters the bucket, so membership is meaningless here; only the live routing target matters", and that is exactly right while a family has ONE target. It misreports when the family has two, which an earlier route reaching into the family is enough to arrange — so the position is not wrong, it is unqualified. Same class as TC-026 and TC-027 (a display deriving eligibility independently of the thing that decides it) and the same root as TC-033, TC-035 and TC-038 (grading on a representative rather than on the ids actually received). Closing it means asking the routing question per id and either marking every account the family reaches or marking none — a display change that wants the sample-root fix under it, round 4a item 1. **Recorded because the enumeration now LISTS it as site S6, and a listing certifies only that the site exists and reads its declared source** — never that what it paints is true. Without this entry a reader of a green enumeration would take the mark for a checked fact | Round 3 pass 19, found while inspecting S6 for the enumeration amendment; reproduced with a live rendered frame at the pass-19 tree AND at the round base `63e6e93` (`r3-finisher/probes/p19-familymark.mjs`, exit 1 at both) — named by sha rather than as "the tip", which is ambiguous the moment a fix lands and read as false in TC-036 for exactly that reason | 2026-08-26 |
| TC-040 | The synthesised-basis mark asked whether a representative is METERED when the sentence it drives claims the representative EXISTS. `basisSynthetic` was set by `FAMILY_MODELS.includes(model)`, and `FAMILY_MODELS` holds ONE REPRESENTATIVE PER SEPARATELY METERED FAMILY rather than a census of real ids. **THE REACH IS STATED AS WHAT IT IS, A CONSEQUENCE OF THE PREDICATE RATHER THAN A CENSUS:** for a wildcard-free glob the strip is the identity, so the representative IS the glob, and the superseded test marked every such glob naming no metered family. That follows from the predicate's form at the line; no enumeration of routes was taken and none is claimed. Reproduced by RENDERING with a live control before it was touched: at `5fd8e9d`, `claude-haiku-4-5 → alpha beta (no id this route receives was measured)` while `modelsForGlob('claude-haiku-4-5')` returns `["claude-haiku-4-5"]` and the wildcard strip changed nothing; the control `claude-*-4` on the same fleet builds `claude--4`, is genuinely fabricated, and is correctly marked. **THE MARK WORKS; THE QUESTION WAS WRONG** — which is why this is a predicate defect and not a mark failing to fire | **FIXED AT `e51b405`, and the durable finding is the SPECIFICATION SHAPE rather than the predicate.** `account-manager.js:1867` at `106bc66` asked the fabrication question directly **— PAST TENSE, because ruling B superseded that predicate too and this entry said "now" for a cycle after it did. A sha-qualified anchor resolves at its own sha; it cannot rescue a sentence whose TENSE claims the present. The live predicate is `account-manager.js:1904-1905` at `2e1ff12` (the `derivedBasis` binding — NAMED because a bare line+sha citation fails silently on the next edit above it, which is exactly how this citation broke: the lines were advanced to the new sha and the label was not, leaving it pointing at a scope object literal. At `da05c45` the same binding is at :1895) and asks about PROVENANCE rather than fabrication; see TC-045 —** and at that sha it read — `scopeGlob && model === stripped && stripped !== scopeGlob`, where `stripped` is the strip's own output at `:1856` — so a wildcard-free glob, whose strip is the identity, cannot be marked. `gpt-*` IS still marked and that is correct under the invariant — MEASURED at `106bc66` rather than carried from the fix's own commit body: a `gpt-*` route publishes `model: "gpt-"` with `basisSynthetic: "unmetered-glob"`, and `gpt-` is not a requestable id, so the mark is true of it. **AN UNDERSPECIFIED INVARIANT INVITES THE IMPLEMENTER'S MECHANISM.** The invariant form — *the mark means this basis is not an id this route can receive; nothing that is such an id may carry it, nothing that is not may lack it* — EXCLUDES this case by construction, because it NAMES THE PROPERTY THAT WOULD FAIL. A specification that stops at the noun leaves the implementer to invent a decidable test, and an invented test cannot be checked against the case it omits. **THE PROVENANCE IS THREE-PART AND IS RECORDED IN ITS RULED ORDER, AGAINST A DEPOSITED ARTIFACT RATHER THAN ANYONE'S MEMORY** — both messages verbatim in `coordination/team-lead-ruling-excerpts-tc038.md`, cited rather than copied here because a quotation reproduced is a quotation that can drift: **(1)** the ruling specified the mark for a scope whose representative is *synthesised* and left the DECIDABLE TEST for that word UNSTATED — an underspecification, the supervisor's; **(2)** the implementation chose `FAMILY_MODELS` membership as that test and justified it as *non-membership implies the fallback* — the conflation, the implementer's, and precisely what the implementer's own FIRST account said, which turns out to have been accurate all along; **(3)** a later supervisory message misquoted the ruling as containing the phrase *not a `FAMILY_MODELS` member*, over-attributing the mechanism to the ruling. **AN EARLIER DRAFT OF THIS ENTRY REPEATED THAT MISQUOTE, AND HOW IT SURVIVED IS THE SECOND DURABLE LESSON: A CONFESSION IS ALSO A CLAIM AND CARRIES THE SAME EVIDENCE OBLIGATION.** The false attribution reached the freeze document, this entry's cause line and both seats' successions, and was accepted by everyone including its own author — precisely BECAUSE it was against interest. Against-interest statements get waved through exactly when they most need checking. It was caught only when a seat declined to write a quotation whose artifact it could not find, which is this round's standing provenance rule reaching a supervisor's confession. **A SPECIFICATION ERROR SURVIVES INTO THE TREE ONLY THROUGH AN IMPLEMENTER WHO DID NOT FALSIFY IT** — that half is unchanged by the correction, and is the author's: `model.js:61-63` at `106bc66` already stated the position the predicate overrode, naming `gpt-*` as a glob that reads as *one scope, on the shared bucket* rather than as an error. Every input was present. **THE JUSTIFICATION-PLACEMENT RULE DID NOT CATCH THIS ONE AND THAT BOUND IS WORTH KEEPING:** the fix's comment COMPLIED — the reasoning was written in the code where an audit reads it — and it was WRONG. Placement makes a justification auditable; it does not make it true. The next layer is that a written justification names the check that could FALSIFY it, and here that check is a rendered witness on a wildcard-free glob, which NO test at `5fd8e9d` performed: all THREE `basisSynthetic` assertions in that tree's suite (`test/decision-block.test.js:1584` and `:1608`, `test/tui-route-glyphs.test.js:433`) run on the wildcard-BEARING `claude-*-4`. An earlier draft of this sentence said "neither of the two TC-038 tests" — a count nobody had taken, in the entry about specifying what was never checked. **THIRD INSTANCE OF THE TWO-QUESTIONS ERROR IN ONE DAY, IN THE SAME EXPRESSION** — presence of a FUNCTION read as provenance of a BEHAVIOUR, then a property of a FUNCTION read as a property of a FIELD, then origin BRANCH read as id REALITY. Each was caught by execution or by another seat and none by care: **AWARENESS IS NOT A CONTROL.** Closes the over-qualification; the figures for a genuinely placeholder basis remain TC-038's residual | Round 3 pass 20, codex and grok independently; reproduced by RENDERING with a live control by the author before it was touched — `r3-finisher/probes/p20-wildcard-free-basis.mjs`, EXIT=1 at `gates-5fd8e9d` and EXIT=0 at `106bc66` with the control still firing at both, so the zero is a fix rather than a dead probe; held by a test and by a sweep row red-verified against it | 2026-08-26 |
| TC-041 | One rendered line carried two disclosure sentences that cannot both be true, and before that a single fabricated scope DENIED a measured sibling. TWO HALVES, ONE ROOT. **(a) THE ERASURE:** the aggregation was `.some(e => e.basisSynthetic)` — an EXISTENTIAL test feeding the ALL-quantified sentence *no id this route receives was measured* — so a route with one real scope and one fabricated one rendered `*fable*, claude-*-4 → alpha beta (no id this route receives was measured)` while `claude-fable-5` was measured and published in the same payload. **(b) THE COMPOSITION:** once the two forms were separated, the mixed route rendered BOTH at once — `(split by model claims; other ids may go elsewhere) (no id this route receives was measured)` — the first asserting the figures are REAL and merely partial, the second that nothing was measured. Same line, same instant, irreconcilable | **BOTH FIXED, (a) AT `e51b405` AND (b) AT `106bc66`, AND ONE SCOPED SENTENCE CLOSES THEM TOGETHER.** The partial case now names the globs whose bases are placeholders — `(no id measured for claude-*-4)` — so nothing the route DID measure is denied, and the unqualified all-quantified form is reserved for the case where every scope is fabricated (`status-renderer.js:918-920` at `106bc66`). **THE SPLIT-QUALIFIER SUPPRESSION THIS ENTRY RECORDED AS THE REMEDY IS WITHDRAWN — see TC-050, and read this sentence as what the tree did at `106bc66` rather than as what it does.** It read: the split qualifier is suppressed when the WHOLE basis is a placeholder (`:1034-1035`), because it has nothing real to be partial about. That followed from the sentence as it THEN read; ruling B replaced it with a provenance claim asserting non-verification rather than non-existence, the collision the suppression prevented stopped arising, and the suppression was left denying true splits on bases that are derived and real. **THIS PARAGRAPH IS THE FOURTH RECURRENCE OF *A CORRECTION ENTERED AS A NEW ENTRY DOES NOT REPAIR THE OLD ONE*, and neither half of the census pair could have found it:** the TEXT half searches the withdrawn CLAIM's wording and this sentence quotes none of it, while the SEMANTIC half reads the moved signal's consumers IN CODE and this is prose. A withdrawn REMEDY is a third thing, and the pair was not looking for it. It was found by asking the question the rule tells you to ask — *what else asserts the thing I just changed* — which is the weakest form of check there is and was the only one that worked here. **THE ERASURE IS FALSE-MEASUREDNESS'S OTHER FACE AND THE MORE DAMAGING DIRECTION:** a false DENIAL rather than a false claim, because a reader told nothing was measured stops looking for the thing that was. **NO PER-FORM CHECK COULD HAVE CAUGHT (b), WHICH IS WHY IT IS REGISTERED SEPARATELY FROM (a):** each sentence is individually TRUE of the scope it describes, so every check that grades one form passes; THE COMPOSITION IS WHAT IS FALSE. The set of sentences on a rendered line must be simultaneously true, and nothing in this round graded a line as a set until now. **THE COMMIT THAT SEPARATED THE FORMS WAS RIGHT AND ITS COMPOSITION CLAIM WAS NOT.** It argued correctly that the synthetic sentence must not fold into `basisGap`, since *split by X* would misdescribe a placeholder basis — and then asserted the two forms *stack, because a route can have a synthesised scope AND a genuinely divided one*. They do not stack; they collide. **A WRITTEN JUSTIFICATION THAT NAMED NO FALSIFIER, IN THE COMMIT THAT COMPLIED WITH THE PLACEMENT RULE** — the mixed fixture WAS the falsifier and nobody had rendered it, which is TC-040's bound arriving a second time by a different route. **SECOND INSTANCE OF THE AGGREGATION FAMILY IN ONE CYCLE, and the census was run rather than assumed:** `basisGap`'s `.find(Boolean)` collapsed distinct gap reasons to the first and was fixed four commits earlier, and this was written beside it. A census over every `.some`/`.find`/`.every` on per-scope truth found NO third instance — and found that `suppressedScopesFor` ALREADY distinguishes `any` from `all` for `figuresAbsent`, the correct pattern, two lines from where the wrong one was written. **THE CODE ALREADY KNEW.** Rule earned: PER-SCOPE TRUTH MAY NOT BE REPORTED WITH AN ALL-QUANTIFIED SENTENCE UNLESS IT HOLDS FOR ALL SCOPES | Round 3 pass 20: (a) from the register's own mechanical audit approaching from the other end, (b) from r3-attestor's independent second. Both reproduced by RENDERING with live controls before either was touched (`r3-finisher/probes/p20-mixed-and-decision.mjs` and `p20-composition.mjs`); r3-attestor graded the scoped sentence as closing both halves at once rather than suppressing one form; two rows red-verified, each dying on the test its label names | 2026-08-26 |
| TC-042 | The caption gate's FIDELITY COMPARISON had never executed once while the gate printed caption verdicts. Shipped at `09c1fdf` under *a gate may not grade a fleet its own capture denies*, it was guarded by `if (captured && captured.band)` — so a capture naming NO ENTRY for the graded model skipped the comparison SILENTLY — and `tools/caption-sample.json` carried no `routing` key at all. On the sample the battery actually runs, for every model, the repair was unreachable. **THE COMPARISON LOGIC WAS RIGHT** — a capture naming the graded model and disagreeing still refused — **SO THIS WAS REACHABILITY, NOT CORRECTNESS**, and that distinction is why the gate looked healthy: every green it produced was real about the checks that ran and silent about the one that did not | **FIXED AT `106bc66` IN TWO INDEPENDENT PARTS, AND NEITHER IS SUFFICIENT ALONE.** The sample gains a `routing` block DERIVED by rebuilding its own fleet through the real `AccountManager` at its own captured clock — never hand-written, because a fabricated block makes the check verify the rebuild against something invented to make it pass, which is the defect one level down. AND both skips are now LOUD: `tools/verify-caption.mjs:357` at `106bc66` refuses a sample with no routing block at all, `:376` refuses one whose routing names no entry for the graded model. Without the second, the next sample captured without a routing block restores the silence, so the data fix alone would have been a fix with an expiry date nobody could see. **UNREACHABLE CODE CANNOT CARRY A CLAIM — SHIPPED AGAIN, IN AN INSTRUMENT, AND UNLABELLED.** `20ec1cf` wrote that rule down about a dead RENDERER branch and KEPT the branch with a comment naming both its unreachability mechanisms and its tripwire; this was the same defect in a GATE, with no label, so it read live to every reader including its author. A dead branch that says it is dead is a tripwire; one that does not is a claim. **THE CAPTION VERDICT THAT EXISTED IS WITHDRAWN RATHER THAN RE-READ, AND THE SEAT THAT HELD NONE SAYS SO RATHER THAN CLAIMING A MATCHING WITHDRAWAL.** r3-attestor withdrew `BLOCK-caption-20ec1cf.log` (1 REPRODUCES / 3 DIFFERS, EXIT=0) from their carried block set with the instruction not to cite it, on the ground that **A GREEN PRODUCED BY AN UNRUN GUARD IS NOT EVIDENCE** — the log is not wrong about the captions it graded, it is simply not evidence that the fleet they were graded against was the captured one. Those are different statements and only the second was ever the gate's job. This seat has no counterpart artifact to withdraw, and that was ESTABLISHED BY r3-attestor2 EXECUTING FOUR CHECKS rather than by either seat interpreting a predecessor sentence: no caption line in ANY of the eight finisher sentinels (not just `5fd8e9d` but `20ec1cf`, the sha their own withdrawn log was taken at and where a counterpart would most likely sit); no caption-named log in this seat's log directory — r3-attestor2 counted 413 files and this seat re-counted 419 after its own run added some, so THE COUNT MOVES AND THE ABSENCE DOES NOT, and the absence is what the claim rests on; no caption verdict anywhere in the freeze, where every mention is narrative or a sweep row; and **the four caption rows in `p4ab-neutralise.py` ARE NOT VERDICTS** — each reverts a fix in `tools/verify-caption.mjs` and watches a test die. **WITHDRAWING A GRADING RUN AND WITHDRAWING A MUTATION ROW ARE NOT THE SAME ACT**, and the distinction is what makes the asymmetry real rather than an accounting gap. The predecessor wording supports the same reading without being rested on: for their own row they named the artifact, the verdict and the exit code; for this seat they wrote only that it was "in the same position" — a claim about STATUS, not a citation of a thing. The caption gate reaches this seat's battery only through `test/caption-gate.test.js` inside the SUITE gate, which is re-taken at every sha like every other clocked gate. Said that way because a symmetrical sentence about both seats would have read as checked while naming an artifact nobody could produce | Round 3 pass 20, r3-attestor (`tools/verify-caption.mjs:341` at `5fd8e9d`), ruled BLOCKING on clause (c) by team-lead; the reachability half reproduced by the author at `5fd8e9d` before it was touched, and the repair graded by r3-attestor's own FIVE-arm acceptance probe (`r3-attestor/probes/r3-pass20-greenwhilered.mjs`) rather than by its author. Arms A, B, C and E must REFUSE and arm D — the shipped sample, as shipped — must remain GRADEABLE, because that is the arm which catches a fix that buys refusal by breaking the gate. Executed at `106bc66` rather than inherited: EXIT=0, all five arms as required, D still grading. **The succession this seat inherited called it a FOUR-arm probe**; it has five, and repeating that without running it would have put a miscount into the freeze's citing document. The ACCEPTANCE is theirs to record at the batch sha; this run only establishes that the claim being repeated is not a relayed one | 2026-08-26 |
| TC-043 | **A SILENT SURFACE IS NOT A NEGATIVE**, recorded as a NEAR-MISS rather than a defect because it was caught before it cost anything, and recorded at all because the cost it nearly had was another seat's finding dismissed on this seat's say-so. Building a fixture for grok's fourth-surface finding — that the Decision block prints figures for a placeholder basis and discloses nothing — the first arrangement printed *no divergence found*, and the report refuting the finding was half written. **THE DECISION BLOCK HAD NEVER RENDERED AT ALL:** `decisionLines` returns `[]` on a passthrough band (`status-renderer.js:286` at `106bc66`, `if (!entry \|\| entry.band.kind === 'passthrough') return [];`) and the fixture had expiry routing off, so every decision was passthrough and the block was empty. Caught only by asking WHY the surface was silent instead of banking the silence | **CLOSED AS A METHOD RULE, AND THE RULE IS THE ENTRY.** Ask why a surface is silent before banking the silence: an empty surface and a surface that disagrees with you produce the same output, and only one of them is a result. The finding was REPRODUCED the moment the band could size, so the near-refutation was wrong on the merits as well as on the method. **IT IS THE MIRROR OF A DISCIPLINE THIS ROUND ALREADY PRACTISES IN THE OTHER DIRECTION** — r3-attestor has twice paid the cost of saying *read-level, not reproduced, do not fix on this* — and the symmetry is the point: an unbuilt fixture is not a refutation any more than an unreproduced report is a defect. **ABSENCE OF A VERDICT IS NOT ABSENCE OF THE DEFECT.** The generalisation is registered separately as TC-044, because the same shape sitting in an INSTRUMENT is a standing hazard rather than a near-miss: this instance was caught by a human asking a question, which is exactly the control that does not scale | Round 3 pass 20, self-caught by the author while building a fixture for grok's fourth-surface finding; the finding itself reproduced once the band could size (`r3-finisher/probes/p20-mixed-and-decision.mjs`, arm B) and is fixed at `e51b405` | 2026-08-26 |
| TC-044 | **A PROBE THAT RENDERS NOTHING AND GRADES THE EMPTINESS REPORTS AGREEMENT IT NEVER OBSERVED.** Crash-safe and vacuity-safe are different properties, and this campaign's rendering probes had only the first. r3-attestor found two in this shape among the rendering probes they audited — eighteen, by their own count, which is reported here as theirs rather than recounted — `r3-pass16-dupjoin-grok.mjs` the sharp one, whose verdict *the line names nobody who cannot serve* is EXACTLY what a line that never rendered produces. A sweep of this seat's probes — 33 files at the time, 24 of them renderer-driving — found SIX more. **THE DIRECTION IS NOT UNIFORMLY THE FLATTERING ONE, WHICH IS WHY "IT WOULD ONLY MAKE US CAUTIOUS" IS NOT AVAILABLE AS A DISPOSITION:** three manufacture a false NOT-REPRODUCED (a defect retired on silence), two a false REPRODUCED (a cycle spent on a fixture that produced no line), and one a false ATTRIBUTION, which is the sixth and is described next. **THE WORST ONE FED THE REGISTER.** `p19-735-mechanism.mjs` identifies WHICH payload field produced a route line's names by matching the rendered name-set against four candidate sources; with no rendered line the set is empty, an empty set equals any empty candidate, and the probe prints *one source matches, so the mechanism is identified* — an attribution from a line that never rendered, in the probe TC-035 cites | **CLOSED, AND THE CITED MEASUREMENT WAS CHECKED RATHER THAN ASSUMED SOUND.** `p19-735-mechanism.mjs` was re-run at the tree TC-035 measured (`gates-6883e25`) before anything was changed: the line DID render, the names are `["repOwner","sibOwner"]`, and the probe reports AMBIGUOUS in the words TC-035 quotes — so the register's claim rests on a real subject and the guard confirms it rather than overturning it. **FOURTEEN SUBJECT-EXISTS GUARDS ARE IN ACROSS EIGHT PROBES, AND NINE DISTINCT REFUSAL PATHS ARE WATCHED REFUSING** — at least one per probe, plus the second guard in the mechanism probe, which grades a different subject (a rendered line that names nobody) and therefore needs its own demonstration. The five not separately demonstrated share the one refusal helper, whose own five absent shapes are watched refusing in `probes/lib-subject-selftest.mjs`; that bound is stated rather than rounded up to "all", sharing one refusal (`probes/lib-subject.mjs`) so the vocabulary lives once, with each call site supplying what its own probe can no longer decide — a refusal that does not say what it cost the reader is a crash with better manners. **DEMONSTRATED IN BOTH DIRECTIONS, WHICH IS THE HALF THAT USUALLY GOES MISSING:** `demo-vacuity-refusals.py` shows each of those nine paths QUIET on a real tree and REFUSING with a named surface on a neutered copy, because a guard that refuses everything is a gate people delete, and this round has already recorded two gates that fired on correct work. **ONE RESULT WAS NEARLY MISREAD AS THE GUARD MISFIRING:** `p19-shared-mark-consumed.mjs` refuses at `106bc66`, which looks like an always-on guard and is not — it refuses on its OWN premise check, because `e51b405` removed the shared-scope false mark it hunts, so it is demonstrated at `gates-5fd8e9d` where the premise is live. **THE SWEEP'S OWN BOUND IS STATED RATHER THAN LEFT TO BE DISCOVERED:** `audit-probe-vacuity.py` matches the guard CALL and not its ARGUMENT, so a probe that guards subject A and grades subject B reads as guarded; that case is pinned in the classifier's self-test so the blind spot is known rather than found later, and the honest reading of the tool is *every renderer-driving probe was looked at*, not *every probe is vacuity-safe* | Round 3 pass 20; the class from r3-attestor's audit of their own eighteen probes, swept over this seat's 33 with `r3-finisher/audit-probe-vacuity.py` (self-tested against five shapes, including a comment-only guard that must NOT count, before it was trusted); 0 candidates / 24 guarded / 11 non-renderers over 35 files at the batch sha (the two added files are the guard and its self-test), with 9 of 9 demonstrated refusal paths quiet on a real tree and refusing when neutered | 2026-08-26 |
| TC-045 | **AN INVARIANT MUST BE DECIDABLE BY ITS ENFORCER.** The synthesised-basis mark was specified as *this basis is not an id this route can receive* — a claim about the WORLD — and no predicate holding only the glob, the branch and the produced string can decide it. **PROVEN BY COLLISION WITNESS, not argued:** `claude-haiku-4-5*` yields a REAL id and `claude-haiku-4-*` yields a fabricated one, and the two agree on every available signal (both take the fallback branch, both have a strip that changed the glob, both have model === strip). Deciding between them needs a census of requestable ids; the repo has `FAMILY_MODELS`, which is THREE ids, one representative per SEPARATELY METERED family, and TC-040 already records that it is not a census. **AND THE SUPERSEDED PREDICATE'S FAILURE CLASS IS UNBOUNDED:** `model === stripped && stripped !== scopeGlob` is a COINCIDENCE detector — it fires whenever stripping the wildcards happens to reconstruct the family representative exactly, which holds for a star inserted between ANY two characters of ANY family id. Enumerated across two seats: 49 of 49 single-star insertions and 120 of 120 two-star combinations. No count of shapes was ever the right instrument | **RULED (team-lead): OPTION B — the CLAIM narrows to what provenance supports, and the family-matched half is adopted outright.** Fixed at the batch sha. `modelsForGlobWithProvenance` (`src/model.js`) carries WHICH BRANCH produced the representative — MATCHED when a metered family answered the glob, DERIVED when the fallback strip did — and `account-manager.js` marks on that fact rather than reconstructing it downstream. **A PREDICATE OVER CONTROL FLOW HAS NO SHAPE SPACE TO ENUMERATE**, which is why this closes all 49, all 120, and every k-star form at once where the two shape-enumerated repairs before it each closed the members somebody had thought of. **THE RENDERED SENTENCE NARROWED WITH IT** and that is the load-bearing half: `(basis derived from the glob, not matched to a metered family)`, with the partial form naming its globs. NON-VERIFICATION, NOT NON-EXISTENCE — the figures are computed for a string this proxy derived, which may or may not be an id anyone requests, and the line no longer pretends to know which. **THE MARKED SET GREW AND THAT IS THE RULING, NOT A REGRESSION:** `claude-haiku-4-5` is marked again, which TC-040 was specifically about NOT doing — but TC-040's defect was marking it with a FALSE sentence, and it now carries a true one. **THE PROVENANCE OF THE OVER-CLAIM IS TEAM-LEAD'S AND IS RECORDED AS SUCH:** the invariant was stated over observables the enforcer does not possess, the third specification failure from that seat on this one mechanism. Rule earned: **BRIEF THE INVARIANT, AND THE INVARIANT MUST BE DECIDABLE BY ITS ENFORCER** — the predicate decides a question about its own control flow, which it always knows, not about the world, which it never does. Options REJECTED and why, kept so they are not re-proposed: a census of real ids is a vocabulary that cannot learn its next member and rots in the flattering direction; asking the routing layer makes 'no traffic yet' indistinguishable from 'fabricated'; dropping the mark discards a true and useful disclosure | Round 3 pass 21, codex and grok on the trailing-star instance; the branch settled by READING which branch runs (`familyModelsMatching('claude-fable-5*')` returns the family member, so it is MATCHED, not a strip) rather than from outputs; domain enumerated by `r3-finisher/probes/p21-glob-shape-census.mjs` (33 globs, 6 bases x 6 shapes, re-keyed to B's criterion and RED at `dfd83ee`, GREEN at the batch sha — a both-ways demonstration by construction); unboundedness enumerated independently by r3-attestor2; held by a property-keyed test and a row red-verified against it | 2026-08-27 |
| TC-046 | **A DISCLOSURE THAT REACHES SOME CONSUMERS AND NOT OTHERS**, four reproduced instances of ONE shape, and the arithmetic is the argument: **TWO COMMITS PRODUCED FIVE FINDINGS**, because each fixed one arm of a disclosure and left its siblings. `106bc66` taught the status line the SOME case and left the DASHBOARD GLYPH on the ALL test (`basisGap \|\| synthetic` — `basisGap` carries FAMILY SPLITS, a different signal, so on a mixed route both are false and the glyph rendered COMPLETE beside a line naming the derived glob). The same commit made two of the caption guard's six control-flow paths loud and left four skipping silently. The DECISION BLOCK's header qualified only the entry that WINS, so a derived SIBLING had no cell that could speak for it, and the sibling map at `:486` read NO disclosure signal at all. And the producer set `basisSynthetic` on BOTH entry paths specifically to prevent field asymmetry — while a renderer EARLY RETURN dropped it one level up, so an all-captured route rendered one of two true facts | **FIXED AS ONE RULE AGAINST A DISCLOSURE-CONSUMER ENUMERATION, not as four patches.** The per-scope truth was always in the payload; what was missing was ONE PLACE THAT STATES THE REDUCTION, so every consumer invented its own and the inventions disagreed. `routeNaming` now publishes `anySynthetic` and the consumers read it. **A GUARD'S BRANCHES ARE A CONSUMER SET** — the caption gate inverts to DEFAULT-REFUSE (a grade is reachable only through a comparison that positively executed AND matched, so no enumeration of bad-band shapes is needed and the shapes nobody has found yet are closed too), and the producer's own early returns carry the reduction. **SETTING THE FIELD IS NOT RENDERING IT.** A comment in the producer said the flag *changes nothing a reader sees* — true of the flag and false of the render — and it is corrected with the fix, because **THE REASSURANCE WAS THE DANGEROUS HALF**: it told the next reader there was nothing downstream to check. The enumeration carries a WHICH-SIGNAL column per consumer, and that column exists because this defect is invisible without it: `tui.js:1278` was in every census run — it is not an unenumerated consumer, it is an enumerated one READING THE WRONG SIGNAL, which a site-list certifying presence and declared-source would pass. Six consumers enumerated, one test each, the unreachable settings-auto branch kept and LABELLED rather than dropped, since a census that omits it hides that it cannot carry a claim | Round 3 pass 21, r3-attestor2 (glyph column, `:486` sibling map, `:743` early return) and codex (the caption guard's silent skips); every one reproduced by RENDERING with a live control before it was touched; enumeration witness at `r3-finisher/probes/p21-disclosure-consumers.mjs`, CLEAN at the batch sha with its all-derived control live; four rows red-verified, each dying on the test its own label names | 2026-08-27 |
| TC-047 | **A READ IS NOT A CHECK**, recorded as a near-miss because it was caught in one cycle, and recorded at all because the cost it nearly had was a supervisor acting on a false clearance. The finishing seat's own field census correctly located the glyph-column asymmetry — `syntheticGlobs` had readers only in `status-renderer` — and the seat then reasoned the gap away with an unverified premise (that a mixed route *sets basisGap*; it does not, `basisGap` carries family splits) and reported NO SILENT SURFACE to team-lead in writing, adding that it had checked. It had not: it read the expression and inferred the value of its operands. r3-attestor2 had already reproduced the same finding independently, so nothing false ever stood in the dispositions | **CLOSED AS A METHOD RULE.** The instrument was right and was OVERRIDDEN — **an enumeration that finds a gap is only as good as the discipline not to reason the gap away**, and *I chased it before reporting it* is exactly what someone says when the chase was a read. Driving the renderer cost ten minutes, as it has every time this round. It pairs with the mirror instance from the other seat, who nearly trusted their own library's return shape without reading it and would have graded a defective tree clean: **A TOOL'S FINDING IS ADJUDICATED BY DRIVING THE SUBJECT, NEVER BY A BELIEF ABOUT THE SUBJECT OR ABOUT THE TOOL.** Both overrides were beliefs; the instruments were right both times. The probe built afterwards carries the author's error in its own header, so the next reader meets the trap rather than the conclusion. **AND THE SWEEP CAUGHT WHAT REVIEW WOULD NOT:** the first version of the glyph consumer's test compared two whole rendered FRAMES and asserted they differed — which they always do, since the fixtures carry different globs and the frame prints them — so it passed on any tree. Its neutralisation row reported SURVIVES, which is the coverage hole announcing itself, and the test now grades the glyph CELL | Round 3 pass 21, self-caught by the finishing author while building the disclosure-consumer enumeration; the finding itself reproduced with a live control at `r3-finisher/probes/p21-sixth-form-glyph.mjs` and independently by r3-attestor2 | 2026-08-27 |
| TC-048 | **THE Other-scopes SIBLING MAP HAS THREE RETURNS AND THE PASS-21 DISCLOSURE FIX TAUGHT ONE.** `status-renderer.js` computed the derived-basis qualifier BELOW both early returns, so a scope disclosed its provenance only when it was neither blocked nor captured. Reproduced by driving the renderer over all three returns (`r3-finisher/probes/p22-sibling-returns.mjs`): a blocked sibling carrying `basisSynthetic: 'unmetered-glob'` rendered `wide (other): blocked` and a captured one rendered `behind (haiku): no figures, an earlier route takes its id`, each dropping the mark the general return had just been taught to print. **SECOND TIME THIS ROUND THAT AN EARLY RETURN HAS BEEN FOUND EXITING PAST A DISCLOSURE BINDING**, and the two are in different functions rather than one branch set: the first was `routeNaming`'s `source: 'none'` returns leaving before the synthetic reduction, fixed one pass ago by the same hoist | **FIXED, AND FROM ONE RULE RATHER THAN PER BRANCH.** The binding is hoisted ABOVE the returns, the shape `routeNaming`'s reduction already uses. The rule is about VERDICTS, not figures: every one of the three verdicts is ABOUT `e.model`, and on a derived scope that string is one the proxy derived rather than one an operator configured — the blocklist is matched against it, the capture test asks whether an earlier route takes it, and the band ranks on it — so all three say so. On the captured cell its absence actively misleads rather than merely omitting: *an earlier route takes its id* reads as a MATCHED representative going elsewhere, when the representative was DERIVED from the glob. (This clause read *when the id is one nobody wrote down* — the withdrawn existence claim in the vocabulary pass 24 found; see TC-054.) `split` is excluded on the captured return alone and the exclusion is DECIDED rather than judged: that return is reached only when the producer set `figuresAbsent: 'representative-captured'`, which `_captureDistortsFigures` gates on `_representativeCaptured` — the first arm of `_familySplit` — so `familySplit` there is `'an earlier route'` or null and nothing else, and rendering it would restate the verdict in a second vocabulary. Its falsifier is asserted in the test, not left in prose | Round 3 pass 22, ruled by team-lead; reproduced and fixed by the finishing seat, whose first version of the covering test PASSED WITH THE CAPTURED FIX REVERTED — the assertion matched a different cell on the same one-line `Other scopes` row, and red-verification is what caught it | 2026-08-27 |
| TC-049 | **THE CAPTION GATE'S FIDELITY COMPARISON READ TWO FIELDS OF A BAND THAT CARRIES NINE, AND ITS OWN COMMENT SAID OTHERWISE.** `tools/verify-caption.mjs` compared `candidates` and `kind` from a hardcoded list while the paragraph above it claimed that *a field nobody added a check for reaches a refusal*. That was false: such a field was IGNORED, silently, which is the exact case the sentence promised to close. Measured on the committed sample — a ladder value altered several levels down (`ladder[1].cumulative`) left both compared scalars in perfect agreement and the gate printed `REPRODUCES` and exited 0, grading a caption against a capture the rebuild contradicts. **THE REASSURANCE WAS THE DANGEROUS HALF** — the same shape as the producer comment that claimed setting `basisSynthetic` *changes nothing a reader sees*, true of the flag and false of the render | **FIXED AT THE CHECK, NOT AT THE SENTENCE.** A comment corrected to describe a two-field comparison would have left the gate as weak and merely honest about it, so the code now meets the specification the comment already stated. **THE FIELD SET IS THE UNION OF THE CAPTURE'S KEYS AND THE REQUIRED LIST**: neither an untaught field nor a DROPPED one passes unexamined. THIS ROW'S ORIGINAL REMEDY — "the field set is derived from the captured artifact: the loop walks the capture's own keys" — WAS WITHDRAWN TWO ROWS LATER BY TC-051, because a guard derived from the artifact cannot see what the artifact omits, and this sentence went on asserting it in the present tense; see TC-053. Eight of the nine fields reconstruct and agree EXACTLY, ladder included — rank, account, bucket, pressure, headroom, cumulative, admission reason. Comparison is ORDER-SENSITIVE for arrays because the ladder is a sequence and an order-blind compare would admit through the fidelity check the very transposition the caption controls exist to catch. ONE exclusion, and it is PARTIAL: `excluded` is compared on its ACCOUNT SET and not on per-verdict reason/bucket/detail. **THE REASON FIRST GIVEN HERE WAS FALSE AND IS CORRECTED**: it said reconstructing the verdicts needs route-scoped `_observeOpts` the rebuild does not construct, but `_availability` DEFAULTS every option, so the gate can call it today — measured, it returns null for all four accounts on the committed sample. That sentence was written from reading a CALL SITE without checking what the CALLEE required. The real reason is stronger: the producer calls with `observe: true` and the scope's ROUTE, both of which the function reads, so the gate's unscoped call is a DIFFERENT COMPUTATION — a disagreement would show this gate asked another question rather than that the capture is unfaithful, and on a default-refuse gate that manufactures refusals on faithful captures — widening it to the whole field would hide a set mismatch and be the old defect again. The gate PRINTS what it compared and what was narrowed, so a future narrowing is visible in the output rather than inferred from the source | Round 3 pass 22, ruled by team-lead; **ACCEPTANCE IS NOT SELF-DISCHARGED AND WAS NOT DISCHARGED WHEN THIS ROW WAS WRITTEN**: the attestation seat's eleven-arm grading closes it, and the ladder-reversal arm now has a subject because a test driving exactly that case is added here. Building it exposed TWO further defects: the rebuild reimplemented `_applyBand` as `decision.keep.map(...)` and reported nobody admitted on a passthrough band, and a gate fixture was found still describing the fleet it had been truncated away from — patched once to satisfy the two-field check and never made coherent | 2026-08-27 |
| TC-050 | **A NARROWED DEFINITION SILENTLY RE-SCOPES EVERY CONDITION WRITTEN AGAINST THE OLD ONE.** `status-renderer.js` suppressed the split qualifier whenever `naming.synthetic` was set. That was CORRECT while the mark claimed the basis was not a requestable id — a placeholder has nothing real to be partial about — and Ruling B narrowed the mark to PROVENANCE without the condition being revisited, leaving it to deny a true division. Reproduced against the control pair by driving the renderer (`r3-finisher/probes/p22-split-suppression-repro.mjs`): `*fable*` and `claude-haiku-4-5*` are split by the same mechanism and differ only in provenance, and the second — whose basis `claude-haiku-4-5` is an id a client can request — had its disclosure suppressed. **NOTHING ABOUT THE LINE WAS STALE TEXT.** It read `!naming.synthetic` before Ruling B and after, mentions no withdrawn phrase, and NO CENSUS OVER THE WITHDRAWN WORDING COULD HAVE FOUND IT | **FIXED BY DELETING THE CONJUNCT, AND THE DIRECTION IS ARGUED RATHER THAN ASSUMED.** No predicate can separate a derived-and-real basis from a derived-and-fabricated one — that undecidability is why Ruling B narrowed the claim at all — so the choice was all-derived-suppressed or all-derived-rendered. Suppression DENIES a division that exists, and a reader told a family is whole stops looking for the part served elsewhere; rendering states a true division beside a neighbouring sentence saying, on the same line, that the basis was derived. The composition rule that produced the suppression is DISCHARGED BY THE REWORDING RATHER THAN ABANDONED: the two sentences collided because the second asserted NON-EXISTENCE, and its replacement asserts non-verification and says nothing about whether the figures are real. Its falsifier is named in the code and pinned negatively in the test — a disclosure that again denies measurement outright would collide exactly as its predecessor did | Round 3 pass 22, graded by r3-attestor2 and ruled by team-lead. **THIS IS WHY THE WITHDRAWAL CENSUS IS NOW A PAIR.** The TEXT half found three rot sites and a tense rot; this is what the SEMANTIC half is for, and re-running `field-census.py` over `synthetic` and its reductions dispositioned every NAMED reader against the narrowed meaning — the census reported no destructured reads for these fields, so for them the named-read set is the whole set the instrument can see, which is the strongest claim it supports — one behavioural defect, this one, plus two bindings still NAMED for the withdrawn claim (`placeholder`, `unmeasuredBasis`), renamed in the same change because a name is what the next reader reasons from and survives every grep for the withdrawn wording. **AND THE CENSUS COULD NOT HAVE FOUND TC-048**, which is worth knowing about this instrument: a field census enumerates READERS, and that defect is a branch which reads the field not at all | 2026-08-27 |
| TC-051 | **A GUARD DERIVED FROM THE ARTIFACT CANNOT SEE WHAT THE ARTIFACT OMITS.** TC-049 replaced the caption gate's hardcoded two-field comparison with a set DERIVED from the captured band — closing the case where a capture ADDS or ALTERS a field, and opening the one where it DROPS one. A key the capture stops sending is not in `Object.keys(cap)`, so nothing iterates it and its absence is never reported; the absence branch survived INSIDE the loop and was unreachable for a genuinely missing field. **REPRODUCED AGAINST THE PREVIOUS TREE RATHER THAN ARGUED**, with the discriminating control: a sample with `band.candidates` deleted REFUSES at `da05c45` (exit 2, *could not RUN … captured value is absent*) and GRADED at `2e1ff12` (exit 0), while the same field PRESENT-but-wrong refuses at BOTH — so the comparison ran and the hole was specifically ABSENCE. Same for `kind`. **AND THE FIDELITY LINE REPORTED *deep-compared 8 of 8 captured band fields*** — full coverage of a set that had silently shrunk, the instrument asserting the one thing that was untrue | **FIXED AS A UNION, WHICH IS THE ONLY SHAPE THAT SEES BOTH DIRECTIONS.** The compared set is the capture's keys UNION the gate's required set: the capture's keys catch what the gate was never taught, the required set catches what the capture has stopped sending, and NEITHER LIST CAN SEE THE OTHER'S BLIND SPOT. Three refusal kinds — UNKNOWN-extra, ABSENT-required, and unequal-value with its type-fault split. Every key of `COMPARE` is required deliberately: both producer band literals emit the same nine top-level fields on every path, verified by command rather than read, with nulls where a variant computes no value — so a missing key is a thinner capture format, which is exactly the state the gate's own paragraph promises will fail loudly. The denominator is now the UNION and the line names what went uncompared. **THE CLASS IS THE FINDING, NOT THE INSTANCE: the repair MOVED THE COMPARED SET RATHER THAN REMOVING THE CLASS.** Its nearest cited precedent is `e51b405`, which fixed one false-mark class and introduced another — stated as the one instance this register can point at rather than as a tally, since a count nobody has enumerated is the construct this row's own audit exists to catch | Round 3 pass 23, r3-attestor3, reproduced independently with live controls on both trees and a differential; graded BLOCKING under clause (c). **THE COMMENT AT `tools/verify-caption.mjs:398-402` (at `e43d94e`, the paragraph beginning *THE COST IS DELIBERATE*) SURVIVED THE EDIT THAT FALSIFIED IT**, still promising that a capture missing a required field refuses loudly — in the same file whose own commit body had just said *THE COMMENT WAS THE DANGEROUS PART: it told the next reader the open case was handled, so nobody looked*. That is the second motivating case for the withdrawal census's OWED third half (TC-050): a property claim in prose that neither census half can reach. Found here only because team-lead ruled a by-hand pass over every comment the fix touches — which also caught a SECOND stale claim in the same block, the sentence still describing the loop as walking `Object.keys(cap)`. **AND THIS ROW ITSELF SHIPPED A CITATION THAT DID NOT RESOLVE** — the very same citation, written without its `tools/` prefix (the bare form is DESCRIBED rather than quoted here, because a checker cannot tell a quotation from a citation and quoting it would re-fire the finding), caught by the register bring-up's decidable half as FILE NOT FOUND and graded blocking on the same standard as TC-038's sha label. The entry recording *the comment survived the edit that falsified it* could not cite the comment. A CITATION IS A CLAIM AND THE CHEAPEST ONE TO CHECK IS WHETHER IT RESOLVES | 2026-08-27 |
| TC-052 | **A COMMIT BODY IS FREEZE-BINDING TEXT AND AN UMBRELLA REACH CLAIM IN ONE CANNOT BE DISCHARGED BY ANY WITNESS.** `2e1ff12`'s body says *The tui.js one sat in the labelled UNREACHABLE settings-auto branch*. `tui.js` HOLDS TWO DISTINCT RENDERERS — the dashboard per-account GLYPH COLUMN (`src/tui.js:1301`, reading `basisGap \|\| anySynthetic`) and the SETTINGS auto-detected block (`src/tui.js:1834`, reading `n.synthetic`) — so a sentence naming one surface where two exist has no fact of the matter about what it claimed, and no rendered witness can settle it. Caught by `r3-attestor/probes/att-reach-audit.py`, which reports it UNRESOLVED | **DISCLOSED AND FROZEN OVER, BECAUSE IT IS STRUCTURALLY UNFIXABLE IN PLACE.** Resolution is a COMMIT-LEVEL fact in that instrument: a later body cannot resolve an earlier body's umbrella, so the only path to a clean audit is AMENDING a landed commit — which the backup-ref convention forbids repointing, and which would destroy the sha the attestation seat pinned its verification of three fixes to. **RULED: DO NOT AMEND** — destroying a verified sha to erase the evidence of a caught ambiguity inverts the audit's point. THIS ROW CARRIES THE PRECISE CLAIM THE BODY COULD NOT: what pass 22 changed in `tui.js` was the SETTINGS block alone, renaming its `unmeasuredBasis` binding to `derivedBasis`; the GLYPH COLUMN was dispositioned by the semantic census as CORRECT under the narrowed meaning and was not touched. The battery row asserts the KNOWN SET — exit 1 with exactly this one named umbrella, failing on a second or on any delta — because a permanently-red gate whose redness carries no information is one people learn to wave through | Round 3 pass 23, r3-attestor3, who also RE-DISPOSITIONED it from an open task to an advisory once they established the body was immutable — the correct call, and it stopped the cycle chasing something no commit could fix. **NAME EVERY SURFACE** is now a body-writing step in the pre-commit sequence, run BEFORE committing, since afterwards the only remedy is amending a sha somebody may already have pinned | 2026-08-27 |

| TC-053 | **A WITHDRAWN CLAIM SURVIVES WHEREVER THE CENSUS CANNOT REACH, AND THIS ONE'S CENSUS COULD NOT SEE A SENTENCE THAT WRAPS.** Pass 23 declined `ebd03b4` on four sites still stating Ruling B's withdrawn meaning in the PRESENT TENSE: TC-049's own remedy clause (withdrawn two rows later by TC-051 and still asserted), `caption-gate.test.js`'s header and test name, `account-manager.js`'s worked example describing the SUPERSEDED strip-change predicate 40 lines above the binding that calls it superseded, and `tui.js`'s `synthetic` DEFINITION with its Ruling-B note attached to a quotation seven lines below rather than to the definition. **A CORRECTION PLACED NEAR A CLAIM DOES NOT REACH IT** — every one had the right version written down somewhere else. | **FIXED, AND THE INSTRUMENT'S BOUND SHIPS WITH THE FIX.** The census reported the tree clean throughout: canonical pattern `live 0 / tombstoned 19`, concept-widened `LIVE 8`. The pattern was never the real limit — `withdrawal-census.py` matches per line, so a claim wrapping mid-phrase is unreachable at ANY width. Across src/test/docs the line-based tool sees 27 sites where a wrap-aware matcher finds 33: ten invisible, eight genuinely new, one of them the `tui.js` definition human review had already graded blocking. **THE BOUND IS \"NO LINE-BASED MATCHER\", NOT \"NO PATTERN\"** — the remedy is a matcher change, not a wider alternation. | Fixed at `5b15701`: all four graded sites plus every previously-invisible ROT occurrence, each correction written AT the claim with the withdrawn form quoted beside it so the next census gets an exact key; occurrences dispositioned per line and re-read at their current numbers. Prototype matcher at `r3-finisher/probes/p24-wrapaware-census.py`; shipped tool deliberately unpatched this cycle, structural fix deferred. Also corrected: a heading claiming TWO wrong predicates preceded the current one where the paragraphs beneath name THREE. **UNTIL THE MATCHER LANDS, A CLEAN CENSUS RUN IS NOT A CLEAN TREE.** | 2026-08-27 |
| TC-054 | **THE REPAIR STRANDED FOUR CITATIONS, SHIPPED A WITHDRAWN REASON AS AN EXECUTABLE STRING, LEFT A TRIPWIRE FOR A REMOVED MECHANISM, AND RESTATED THE WITHDRAWN CLAIM IN NEW VOCABULARY.** Pass 24 declined `5b15701` on six items, four of them caused BY the TC-053 fix rather than surviving it. `src/tui.js:1294` and `:1827` and `src/account-manager.js:2043` — the last cited TWICE from `tools/verify-caption.mjs` — resolved exactly at `ebd03b4` and landed on comments here, drifted by edits ABOVE them. **`ebd03b4`'S OWN BODY IS \"a citation is a claim, and the cheapest one to check is whether it resolves\", AND IT ADOPTED THE THREE-PART FORM BECAUSE \"a bare line citation fails silently on the next edit above it\"** — the next commit made the edit above and four failed silently. | **THE SHARPEST ITEM IS THE VOCABULARY ONE, AND NO CENSUS COULD HAVE FOUND IT.** `test/decision-block.test.js` configures `claude-haiku-4-5` explicitly TWICE and then asserts a message reading \"an id nobody configured\", beside `status-renderer.js` saying \"the id is one nobody wrote down\" — Ruling B's withdrawn EXISTENCE claim in words no pattern of ours keys on, false of the very fixture that drives it. Wrap-awareness does not help, because the problem is the WORDS and not the wrapping: **A CLASS DOES NOT END WHEN ITS KNOWN SPELLINGS ARE EXHAUSTED.** Also: `verify-caption.mjs` shipped the withdrawn `excluded` narrowing as a LIVE STRING the gate prints, with its own refutation twenty-five lines above it — every other instance of this class this round was prose. | Fixed at the fourteenth candidate: citations re-pointed and made content-bearing (each names its expression, not only its line); the `narrowed` string restated as scoped-vs-unscoped-are-different-computations, matching the correction already in the file; the spent tripwire recorded rather than deleted, because A TRIPWIRE FOR A REMOVED MECHANISM IS AN INSTRUCTION TO RE-ADD IT; the existence vocabulary replaced with provenance in both places. **AND THE TABLE GATE REGRESSED: TC-053 SHIPPED AS A TWO-COLUMN ROW** where the schema wants five — `check-residuals-table.py` green at `ebd03b4`, red here, and `5b15701`'s body listed suite, lint, typecheck and census while omitting the one gate that failed. | 2026-08-27 |

Not residuals (never report, no ID needed): anything `npx eslint .`
enforces.
